# BUG-260612.35: Dead conditions and stale comments left by past refactors

## Status: Fixed
## Severity: Low
## Version: v2.5.6
## Found In: REVIEW-260612.1 #L19 — `NanobotSystem.Welding.cs:294, 322`, `NanobotSystem.Scanning.Block.cs:367`

## Description
Housekeeping: (1) the weld exhausted-mark condition still contains `!welding && totalComponentChecks == 0`, which are compile-time-constant at that point since BUG-135 moved the weld work after the lock — the live guard is `!needWelding && !hadLockOn`, and the dead terms misstate the invariant; (2) `if (!transporting)` at `Welding.cs:322` is always true; (3) the comment at `Scanning.Block.cs:367` references "line 260" — the gate moved to ~278.

## Root Cause
Refactors left stale guards/comments behind.

## Fix
Simplify the exhausted-mark condition to its live terms, drop the always-true check, fix the comment reference.

**Resolution:** commit 643ef58 (BuildId 260612.7) on `fix/v2.5.6` - constant-false terms dropped from the weld exhausted-mark condition (now !needWelding && !hadLockOn); stale "line 260" comment fixed. The always-true if(!transporting) guard was deliberately left — harmless, and removing it risks more than it saves. Build: Debug, 0 errors.
