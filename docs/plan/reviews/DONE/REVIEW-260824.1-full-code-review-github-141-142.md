# REVIEW-260824.1: Full code review + validation of GitHub issues #141 / #142

## Status: Done
## Reviewer: AI (multi-agent verified review, high effort)
## Date: 2026-08-24
## Version: v2.5.6
## Scope: Full mod source (`SKO-Nanobot-BuildAndRepair-System/Data/Scripts/...`), companion script, GitHub issues #141 and #142.

## Summary
Two reported GitHub issues validated as real bugs with root causes identified. The broad
review produced 13 confirmed correctness findings (10 reported after severity cap, 2 refuted
during verification, 1 plausible), all triaged into BUG tickets below.

## Findings

| # | Finding | Outcome |
|---|---------|---------|
| 1 | GitHub #141: components with unit volume > `_MaxWeldTransportVolume` floor to 0 in both pull paths — never welded, push-immediately ejects them (RadioCommunication 70 L etc.) | BUG-260824.1 |
| 2 | GitHub #142/#83: `State.MissingComponents` only holds the single chosen weld target's shortfall; aggregation over all targets lost vs v2.2.x — companion script queues 5-6 items or nothing | BUG-260824.2 |
| 3 | `NetworkMessagingHandler.Unregister` keeps the null-guard-before-flag-reset latch (BUG-260610.27 pattern) — next world gets no network handlers | BUG-260824.3 |
| 4 | Friendly grind-damage suppression only works when the attacker owns a BaR (`FriendlyRelationsHandler` keyed by welder owners only) | BUG-260824.4 |
| 5 | Transport-start test `remainingVolume < _MaxWeldTransportVolume` fires on leftover transport items — phantom transports hold assignments on unweldable blocks | BUG-260824.5 |
| 6 | `SharedEntityCache` verifies only 50 m-quantized keys — different-size scan boxes in the same cell share cached entity lists | BUG-260824.6 |
| 7 | Deformation-only repair lost: scan can't flag full-integrity dented blocks (allowMutate=false) and no main-thread path picks them up | BUG-260824.7 |
| 8 | 500 ms settings rate limit keyed per-block drops a second authorized client's change permanently (dirty bit already cleared client-side) | BUG-260824.8 |
| 9 | Foreign-claimed lock-on block: weld loop skips all targets each tick; BUG-260610.32 branch is dead code for this race | BUG-260824.9 |
| 10 | `EnsureQueued` exposed under `ScriptControllFixed` as "read-only" but mutates assembler queues (no ownership check) | BUG-260824.10 |
| 11 | Terminal-controls init retry re-adds every already-registered control/action (engine API is append-only) | BUG-260824.11 |
| 12 | RazeQueueHandler mech-deferral re-dequeued within the same drain, starving non-mech razes | BUG-260824.12 |
| 13 | `_rescanForced` latch claim | Refuted during verification — factually wrong |
| 14 | Weld-side target-zone gap | Refuted — documented Won't Fix in BUG-260612.4 |

## Notes
Confirmed-but-cut lower-severity items (not ticketed this round; revisit if touched):
- Grind-slot refund gaps on early returns other than the fixed ones.
- `GetTransmitFull` broadcast starvation scenario.
- SafeZone caching perf gaps.
- Cleanup: dead `lock(cluster.Members)` blocks, duplicated push-eligibility classification
  (ServerTryPushInventory vs ServerEmptyTransportInventory), hand-rolled cache keying.

## Action Items
- [x] Fix BUG-260824.1 / .2 (GitHub #141, #142 — user-facing, High). Commit 48de267, BuildId 260824.1.
- [x] Fix BUG-260824.3-.5 (High). Commits 48de267 / 81561a9.
- [x] Fix BUG-260824.6-.10 (Medium). Commit 81561a9, BuildId 260824.2.
- [x] Fix BUG-260824.11-.12 (Low). Commit 81561a9, BuildId 260824.2.
