# State — Code Review Tracking

Master index for all bugs, features, and reviews.

Items in `TODO/` are pending. Items in `DONE/` are completed.

## Reviews

| ID | Topic | Date | Version | Status |
|----|-------|------|---------|--------|
| REVIEW-full-code-review | Full codebase code review | 2026-03-12 | v2.5.0 | Done |
| REVIEW-profiling-118bars-120s | Profiling analysis — 120s, 118 BaRs, healthy verdict | 2026-03-18 | v2.5.0 | Done |
| REVIEW-extended-code-review-v250 | Extended code review — duplications, dead code, quality | 2026-03-23 | v2.5.0 | Done |
| REVIEW-full-code-review-v250-r3 | Full code review round 3 — correctness, thread safety, edge cases | 2026-03-25 | v2.5.0 | Done |
| REVIEW-full-code-review-v250-r4 | Full code review round 4 — pre-merge review of fix/new_v2.5.0 | 2026-03-26 | v2.5.0 | Done |
| REVIEW-full-code-review-v250-r5 | Full code review round 5 — threading, null safety, performance (BUG-065..071) | 2026-03-26 | v2.5.0 | Done |
| REVIEW-full-code-review-v250-r6 | Full code review round 6 — dead code, exception handling, input validation, atomicity (BUG-072..083) | 2026-03-26 | v2.5.0 | Done |
| REVIEW-260501.1 | Structure / cleanup / consolidation / reusable refactor / performance (35 findings, 27 implemented + 8 deferred) | 2026-05-01 | v2.5.4 | Done |
| REVIEW-260502.1 | Full Codex code review (8 findings — 7 triaged into BUG tickets, 1 dismissed as theoretical) | 2026-05-02 | v2.5.4 | Done |

## Bugs

| ID | Severity | Version | File | Status | Description |
|----|----------|---------|------|--------|-------------|
| BUG-001 | High | v2.5.0 | `DamageHandler.cs:60` | Done | `GetValueOrDefault()` C# 6 compilation error |
| BUG-002 | High | v2.5.0 | `NanobotSystem.Scanning.cs:66,371` | Done | Async flag race condition — scan updates silently dropped |
| BUG-003 | Medium | v2.5.0 | `TtlCache.cs:101-112` | Done | `CleanupExpired()` live enumeration + removal |
| BUG-004 | Medium | v2.5.0 | `PriorityHandling.cs:73,83` | Done | `_PrioHash` indexer `KeyNotFoundException` risk |
| BUG-005 | Medium | v2.5.0 | `BlockSystemAssigningHandler.cs:9` | Done | `IMySlimBlock` reference equality as cache key |
| BUG-006 | Low | v2.5.0 | `SafeZoneHandler.cs:19` | Done | `Zones` dictionary potential memory leak |
| BUG-007 | High | v2.5.0 | `NanobotSystem.Inventory.cs`, `Scanning.cs`, `InventoryHelper.cs` | Done | Push targets include other BaR inventories — circular transfers and inventory-full gridlock |
| BUG-008 | Medium | v2.5.0 | Terminal UI sort toggles | Done | Deselecting a sort toggle (Nearest/Farthest/Smallest Grid) clears all sort options |
| BUG-009 | Medium | v2.5.0 | `Effects.cs` | Done | Sound effects emitted at player position instead of BaR block |
| BUG-010 | Medium | v2.5.0 | `NanobotSystem.CustomInfo.cs`, `SyncBlockState.cs` | Done | SafeZone/Shield false warnings on init — bools default to false before first check |
| BUG-011 | Medium | v2.5.0 | `UtilsInventory.cs`, `NanobotSystem.Inventory.cs` | Done | No guard for deleted inventory owners in push/pull operations |
| BUG-012 | Low | v2.5.0 | `NanobotSystem.Operations.cs`, `NanobotSystem.State.cs`, `SyncBlockState.cs` | Done | Custom info panel shows "Grinding (Transporting)" when only collecting floating objects |
| BUG-013 | Low | v2.5.0 | `NanobotSystem.Init.cs`, `PowerHelper.cs` | Done | Max power shown in custom info panel is incorrect (350 kW instead of 200 kW) |
| BUG-014 | High | v2.5.0 | `NanobotSystem.Operations.cs`, `Scanning.cs` | Done | BaR attempts weld/grind/collect before initial scan completes — false "Missing Components" |
| BUG-015 | Medium | v2.5.0 | `NanobotSystem.Operations.cs` | Done | BaR grinds/collects when welder inventory is full (only transport checked) |
| BUG-016 | Medium | v2.5.0 | `NanobotSystem.Inventory.cs`, `Scanning.cs` | Done | Constant push attempts to full push targets — no backoff |
| BUG-017 | High | v2.5.0 | `Mod.cs`, `NanobotSystem.Operations.cs`, `Grinding.cs`, `Welding.cs` | Done | CountSystemsOnGrid main-thread performance bottleneck |
| BUG-018 | High | v2.5.0 | `NanobotSystem.Operations.cs` | Done | Secondary task blocked in WeldBeforeGrind/GrindBeforeWeld when all primary targets hit MaxSystemsPerTargetGrid |
| BUG-019 | Medium | v2.5.0 | `UtilsSorting.cs`, `NanobotSystem.Scanning.cs` | Done | Grind sorting priority not fully respected — wrong filter + pre-sort cap truncation |
| BUG-020a | High | v2.5.0 | `SafeZoneHandler.cs:446` | Done | Null reference crash on `safeZoneBlock` lookup — falls through to "not protected" |
| BUG-020b | Medium | v2.5.0 | `NanobotSystem.Scanning.cs` | Done | Floating objects collected outside working area — no OBB containment check |
| BUG-021 | High | v2.5.0 | `DamageHandler.cs:109-118` | Done | Thread-unsafe `FriendlyDamage` dictionary + `Mod.NanobotSystems` iteration without lock |
| BUG-022 | Medium | v2.5.0 | `BlockPriorityHandling.cs:68,98` | Done | `GetItemKey` cache ignores `real` parameter — mis-prioritized blocks for 5 min |
| BUG-023 | Medium | v2.5.0 | `UtilsSorting.cs:108` | Done | Silent `catch {}` swallows all sorting exceptions — hides bugs |
| BUG-024 | Medium | v2.5.0 | `UtilsSorting.cs:31` | Done | `Welder.WorldAABB` accessed from background thread — inconsistent distance sort |
| BUG-025 | Low | v2.5.0 | `Logging.cs:113` | Done | Constructor uses `_ModName` before assignment — null in init log message |
| BUG-026 | Medium | v2.5.0 | `SafeZoneHandler.cs:163-181` | Done | LINQ allocations in `GetSafeZonesInRange` — per-block allocs during weld/grind |
| BUG-027 | High | v2.5.0 | `NanobotSystem.Scanning.cs` | Done | Scan budget starvation — single large grid fills scan cap, BaRs idle on other grids |
| BUG-028 | Critical | v2.5.0 | `NanobotSystem.Welding.cs` | Done | Lock-on lost after scan rebuild — IMySlimBlock reference mismatch causes partial welding |
| BUG-029 | High | v2.5.0 | `NanobotSystem.Welding.cs`, `Grinding.cs` | Done | Assignment leak during component starvation — BaRs lock all targets to themselves |
| BUG-030 | Medium | v2.5.0 | `NanobotSystem.Welding.cs` | Done | Projected→physical assignment gap — other BaRs can steal block during stagger wait |
| BUG-031 | Low | v2.5.0 | `Mod.cs` | Done | Auto stagger counts disabled BaR blocks — inflated stagger with few active BaRs |
| BUG-032 | Critical | v2.5.0 | `Chat/Commands/ConfigCommand.cs` | Done | ConfigCommand uses System.Reflection — SE sandbox violation |
| BUG-033 | Critical | v2.5.0 | `NanobotSystem.Grinding.cs:37-44` | Done | Null dereference when targetData.Block is null — crash |
| BUG-034 | Medium | v2.5.0 | `Collections/*HashList.cs, *HashDictionary.cs` | Done | GetSyncList off-by-one: sends MaxSyncItems+1 vs hash of MaxSyncItems |
| BUG-035 | High | v2.5.0 | `Mod.cs:204-213` | Done | Busy-wait infinite loop in UnloadData with no timeout |
| BUG-036 | Low | v2.5.0 | `SafeZoneHandler.cs:36` | Done | Typo "ProtectedFromGindingCache" — missing 'r' in Grinding |

## Features

| ID | Priority | Version | Status | Description |
|----|----------|---------|--------|-------------|
| FEAT-001 | Medium | v2.5.0 | Done | Built-in method profiler with chat commands and auto-stop |
| FEAT-002 | Medium | v2.5.0 | Done | Dynamic MaxSystemsPerTargetGrid default based on game type |
| FEAT-003 | Medium | v2.5.0 | Done | Welding loop perf: AssignToSystem early-out + Ignore preservation |
| FEAT-004 | Medium | v2.5.0 | Done | Custom info panel periodic refresh every 2s |
| FEAT-005 | Low | v2.5.0 | Done | Reduce source/push target scan interval from 60s to 30s |
| FEAT-006 | Medium | v2.5.0 | Done | Decouple source/push-target scanning from target scanning |
| FEAT-007 | Medium | v2.5.0 | Done | Work-mode-aware scan stopping |
| FEAT-008 | High | v2.5.0 | Done | Cluster coordinator — shared scanning for co-located BaR systems |
| FEAT-009 | Medium | v2.5.0 | Done | Debug mode toggle for terminal custom info panel diagnostics |
| FEAT-010 | Medium | v2.5.0 | Done | Profiling additions & cache hit/miss logging for SafeZone handlers |
| FEAT-011 | Medium | v2.5.0 | Done | Add Cryo/Refinery as source/push targets |
| FEAT-012 | High | v2.5.0 | Done | Grinding performance optimizations (mechanical throttle, staggering, grind budget, sort fix, cache TTL) |
| FEAT-013 | Medium | v2.5.0 | Done | Per-cluster stagger with gradual ramp, capped at 3 groups (~500ms max) |
| FEAT-014 | Medium | v2.5.0 | Done | Sim-speed adaptive throttle & profiler sim-speed tracking |
| FEAT-015 | Medium | v2.5.0 | Done | Admin-only `/nanobars sim` command to override sim-speed for testing |
| FEAT-016 | Medium | v2.5.0 | Done | Cluster pre-sort optimization — eliminate redundant per-member sorts (40% reduction in ApplyClusterResultToSelf) |
| FEAT-017 | Medium | v2.5.0 | Done | Empty grid rescan delay — skip grids with no targets for configurable duration (default 30s) |
| FEAT-018 | Medium | v2.5.0 | Done | Improve sorted block cache hit rate — closed, sorted cache removed entirely by FEAT-027 |
| FEAT-019 | Medium | v2.5.0 | Done | AsyncAddBlocksOfBox scan optimization — closed as superseded (early-out already implemented) |
| FEAT-020 | Low | v2.5.0 | Done | ServerTryWeldingGrindingCollecting main-thread cost reduction — deferred, sim speed stable at 1.0 |
| FEAT-021 | Low | v2.5.0 | Done | AsyncScanForSources cost reduction — closed, cost dropped to 25.7ms total (negligible) |
| FEAT-022 | Medium | v2.5.0 | Done | Change Mod.NanobotSystems to ConcurrentDictionary — eliminate inconsistent locking |
| FEAT-023 | High | v2.5.0 | Done | Configurable stagger, grind budget, and assignment TTL via chat commands and ModSettings.xml |
| FEAT-024 | Medium | v2.5.0 | Done | Config reload and reset commands (`/nanobars config reload`, `/nanobars config reset`) |
| FEAT-025 | Medium | v2.5.0 | Done | Profiler auto-stop sends completion feedback to DS clients via network messaging |
| FEAT-026 | Medium | v2.5.0 | Done | Idle weld loop skip — hash-based early-exit for saturated BaRs (25% ServerTryWelding reduction) |
| FEAT-027 | High | v2.5.0 | Done | Remove redundant full-grid sort from scan pipeline — sort only qualifying candidates (45% main-thread reduction, 39% scan reduction) |
| FEAT-028 | Medium | v2.5.0 | Done | Pool TruncateGridAware allocations — 16% ApplyClusterResultToSelf reduction, eliminates 9000+ heap objects |
| FEAT-029 | Medium | v2.5.0 | Done | Add profiling to UpdateCustomInfo — revealed 22% of main-thread budget (3.44ms/s) was invisible |
| FEAT-030 | Medium | v2.5.0 | Won't Fix | Cache per-grid relation lookups — invalid, blocks can have different owners than the grid owner |
| FEAT-031 | Low | v2.5.0 | Done | Move per-block scenario/immunity checks outside loop — 12% AsyncAddBlocksOfGrid reduction |
| FEAT-032 | Medium | v2.5.0 | Done | Reduce UpdateCustomInfo cost — 2s interval (was 1s) + skip TriggerTerminalRefresh on dedicated servers |
| FEAT-033 | Low | v2.5.0 | Done | Move cache classes (TtlCache, SharedGridBlockCache, SharedEntityCache) to Caches/ folder and namespace; remove empty Managers/ |
| FEAT-034 | Medium | v2.5.0 | Done | Weld mode dropdown — Full / Functional / Skeleton combobox + integrity cap fix for non-Full modes |
| FEAT-035 | High | v2.5.0 | Done | Weld loop grid-level skip — precomputed saturated grid HashSet eliminates per-block dictionary lookups |
| FEAT-036 | High | v2.5.0 | Done | ServerDoGrind spike mitigation — time-based grind budget (8ms/tick cap) + sub-timing profiling |
| FEAT-037 | Medium | v2.5.0 | Done | ServerTryPushInventory optimization — adaptive push interval (5s→10s when <75% full), ~50% call reduction during grinding |
| FEAT-038 | Medium | v2.5.0 | Done | MsgBlockStateSend optimization — progressive backoff (1-2s→4-8s) via state fingerprint, ~50-60% transmit reduction |
| FEAT-039 | Low | v2.5.0 | Done | Idle BaR early exit — skip sub-method dispatch in ServerTryWeldingGrindingCollecting for 0-target BaRs (~6ms/s) |
| FEAT-040 | Low | v2.5.0 | Done | Background scan cost scaling — grid AABB containment pre-check skips per-block OBB range checks (~50ms/scan for contained grids) |
| FEAT-041 | Medium | v2.5.0 | Reverted | Idle scan acceleration — shorter scan interval (4s) when idle+AllowBuild for faster projector detection. Reverted: caused 4s spikes |
| FEAT-042 | Medium | v2.5.0 | Done | ServerFindMissingComponents optimization — single GetMissingComponents call for projected blocks (creation component picked first, then remaining) |
| FEAT-043 | Low | v2.5.0 | Done | Profiling additions — ServerDoWeld sub-timing (buildMs/stockpileMs/mountMs), ServerPickFromWelder profiling, Mod.UpdateBeforeSimulation per-frame profiling |
| FEAT-044 | Medium | v2.5.0 | Done | Delta state sync — skip unchanged lists via ExcludedLists bitmask + hash comparison, full sync every 5th transmit. ~80% of sends skip at least some lists |
| FEAT-045 | Low | v2.5.0 | Done | Reduce MaxSyncItems from 64 to 24 — cuts target list payload ~63% per sync message |
| FEAT-046 | Low | v2.5.0 | Done | TryTransmitState profiling — logs skip/send actions, fingerprint changes, backoff level, and excluded lists bitmask |
| BUG-037 | Medium | v2.5.0 | Done | Null CubeGrid in IsSameBlock — guard against null CubeGrid on blocks from closing grids |
| BUG-038 | Medium | v2.5.0 | Done | Null definition in EmptyBlockInventories — guard against null from GetPhysicalItemDefinition |
| BUG-039 | Medium | v2.5.0 | Done | Unguarded projector grid access in ServerDoWeld — null check after proj.Build before accessing projector CubeGrid |
| FEAT-047 | High | v2.5.0 | Done | RebuildClusters skip-when-unchanged — cache cluster key per BaR, skip full rebuild when no keys/count changed. Eliminates 28ms spikes at 234 BaRs |
| FEAT-048 | Medium | v2.5.0 | Superseded | BuildGridSystemCountCache throttle — superseded by BUG-052 live counter (cache removed entirely) |
| BUG-040 | High | v2.5.0 | Done | Safe zone cluster split — include SafeZoneAllowsWelding/Grinding in cluster key so BaRs inside vs outside safe zones get separate coordinators |
| BUG-041 | Critical | v2.5.0 | Done | Projected block welding broken by FEAT-042 — creation component not guaranteed first in transport, restored two-step pick order |
| BUG-042 | High | v2.5.0 | Done | Grinding loop missing CubeGrid null guard — crash on blocks from closing grids (line 43, 125) |
| BUG-043 | Medium | v2.5.0 | Done | SyncBlockState.AssignReceived Position.Value without HasValue check — client crash on corrupted sync |
| BUG-044 | Medium | v2.5.0 | Done | Effects.cs uses System.Threading.Interlocked — SE sandbox violation, replaced with plain ++/-- |
| BUG-045 | Medium | v2.5.0 | Done | TtlCache accesses MyAPIGateway.Session without null guard — crash during shutdown |
| BUG-046 | Low | v2.5.0 | Done | BlockPriorityHandling cache stale for 5 min on block Enabled state change — TTL reduced to 30s |
| BUG-047 | Low | v2.5.0 | Done | SafeZoneHandler returns closed/deleted zones from cache — added Closed/Enabled check |
| BUG-048 | Low | v2.5.0 | Done | Welding assignment not released for non-weldable blocks — moved release outside Ignore branch |
| BUG-049 | Medium | v2.5.0 | Done | Script priority list parsing crashes on malformed data — added values.Length >= 2 guard |
| BUG-050 | Medium | v2.5.0 | Done | Script empty DisplayKinds array causes DivideByZero — added Length > 0 check |
| BUG-051 | Low | v2.5.0 | Done | Script BlockName IMyCubeGrid branch unreachable — swapped order with IMyEntity |
| BUG-052 | High | v2.5.0 | Done | MaxSystemsPerTargetGrid overshoot — live counter replaces stale cache, lock-on blocks now enforce grid limit |
| FEAT-049 | Medium | v2.5.0 | Done | TextHudAPI debug/profiling HUD overlay — admin-only, two-column layout, comprehensive stats, soft dependency on BuildInfo |
| FEAT-050 | Low | v2.5.0 | Done | Debug HUD position command — `/nanobars debug [left\|right]` to position overlay on either side of screen |
| FEAT-051 | Low | v2.5.0 | Done | Hide custom info debug section on dedicated servers — skip terminal debug diagnostics when IsDedicated |
| FEAT-052 | Low | v2.5.0 | Done | Remove "Help Others" from terminal — hidden, forced false, no functional use for the mod |
| FEAT-053 | Low | v2.5.0 | Done | Rename "Build new" to "Build Projections" + reorder welding section (AllowBuild and WeldMode at top) |
| BUG-053 | High | v2.5.0 | Done | BaRs on partially safe-zone-covered grids don't split into separate clusters correctly |
| BUG-054 | High | v2.5.0 | Done | Block assignments growing — two leaks: projected→physical transition + lock-on vanished retry path |
| BUG-055 | Low | v2.5.0 | Done | Debug HUD SafeZoneBlocked not counting building projections disabled |
| BUG-056 | Low | v2.5.0 | Done | Reset All Settings does not restore priority list order — only enabled state was reset |
| BUG-057 | Medium | v2.5.0 | Done | PullComponents allocates new List per source inventory iteration — GC pressure in welding hot path |
| BUG-058 | Medium | v2.5.0 | Done | PreSortClusterCandidates allocates two distance dictionaries per scan cycle — reuse single dict |
| BUG-059 | Low | v2.5.0 | Done | isDedicated detection wrong in HelpCommand — DS note shown to all non-host clients |
| BUG-060 | Low | v2.5.0 | Done | Grid cost timestamp inflated by StopAndLog overhead in AsyncAddBlocksOfGrid |
| BUG-061 | Low | v2.5.0 | Done | Dead code IsBuildingBlockedAtPosition in SafeZoneHandler — never called |
| BUG-062 | Low | v2.5.0 | Done | Orphaned XML doc comment — double summary on IsProjectorGridBuildBlocked |
| BUG-063 | Low | v2.5.0 | Done | Indentation mismatch in Mod.RebuildSourcesAndTargetsTimer try block |
| FEAT-054 | Low | v2.5.0 | Done | Admin welcome message on session join — notifies about /nanobars help |
| FEAT-055 | Low | v2.5.0 | Done | Show mod version in debug HUD overlay and terminal custom info panel |
| FEAT-056 | Low | v2.5.0 | Done | `/nanobars profile summary` — live profiling summary in mission screen dialog (admin-only) |
| FEAT-057 | Low | v2.5.0 | Done | Show ModSettings.xml loaded status in debug HUD and terminal info |
| FEAT-058 | Low | v2.5.0 | Done | "Powered by SKO85" footer in debug HUD overlay |
| FEAT-059 | Low | v2.5.0 | Done | Extract SendToAdmins helper with reusable player list — deduplicate admin broadcast |
| FEAT-060 | Low | v2.5.0 | Done | Sim-speed min/avg in profile summary HUD panel |
| FEAT-061 | Low | v2.5.0 | Done | Debug command hint when local HUD is hidden |
| BUG-064 | Low | v2.5.0 | Done | Debug HUD scan age inflated by disabled/unscanned BaRs (showed 590s+) |
| FEAT-062 | Medium | v2.5.0 | Done | Per-session profiling with named sessions, list, and clear commands |
| FEAT-063 | Low | v2.5.0 | Done | Custom info panel cleanup — remove redundant debug items, restrict to local game only |
| FEAT-064 | Medium | v2.5.0 | Done | Admin commands for remote BaR system management (`/nanobars systems list/count/enable/disable`) |
| BUG-065 | High | v2.5.0 | Done | Missing volatile on cross-thread flags (_AsyncUpdateSourcesAndTargetsRunning, _InitialScanCompleted, _PushTargetsFull, AssignedCluster, _sharedResult) |
| BUG-066 | High | v2.5.0 | Done | Weld exhaustion hash read/write outside lock — TOCTOU race with background scan |
| BUG-067 | High | v2.5.0 | Done | Division by zero in ServerDoGrind on modded blocks with zero DisassembleRatio/MaxIntegrity |
| BUG-068 | Medium | v2.5.0 | Done | Missing null guards: MyCubeGrid cast in ServerFindMissingComponents, Entity in ServerDoCollectFloating |
| BUG-069 | Medium | v2.5.0 | Done | Close() clears state while async scan may still be running — use-after-close race |
| BUG-070 | Medium | v2.5.0 | Done | Profiler lambda closures allocated every tick even when profiling is off — GC pressure |
| BUG-071 | Low | v2.5.0 | Done | PullComponents allocates new List per call + LINQ .Count() on dictionary |

| BUG-072 | Critical | v2.5.0 | `Utils/Utils.cs:35-41` | Done | Dead code: inner `MaxDeformation < MinDeformation` check unreachable inside `> MinDeformation` block — deformation tracking never updates |
| BUG-073 | Critical | v2.5.0 | `NanobotSystem.Init.cs:170-171` | Done | Close() spin-wait on `_AsyncUpdateSourcesAndTargetsRunning` without lock — may not observe background thread's write, risking use-after-free |
| BUG-074 | High | v2.5.0 | `NanobotSystem.State.cs:65-97` | Done | Silent `catch {}` in IsShieldProtected/IsWelderShielded — shield check failures invisible |
| BUG-075 | High | v2.5.0 | `NanobotSystem.Welding.cs:383-387` | Done | Silent `catch {}` in IsWeldIntegrityReached returns true — silently skips incomplete blocks |
| BUG-076 | High | v2.5.0 | `Chat/Commands/ConfigCommand.cs:313-327` | Done | IntSetting/FloatSetting accept any parsed value — no bounds validation on chat config commands |
| BUG-077 | Medium | v2.5.0 | `Mod.cs:43-59` | Done | DecrementGridCount TryGetValue→TryUpdate non-atomic — second decrement silently fails, count drifts |
| BUG-078 | Medium | v2.5.0 | `Caches/SharedEntityCache.cs:33,100` | Done | GetEntitiesInBox/Cleanup access Session.ElapsedPlayTime without null check — crash during load/unload |
| BUG-079 | Medium | v2.5.0 | `Chat/Commands/SystemsCommand.cs:68-73` | Done | Empty `--owner`/`--grid` filter produces empty string — IndexOf matches all players/grids |
| BUG-080 | Medium | v2.5.0 | `Handlers/GridOwnershipCacheHandler.cs` | Done | Multiple bare `catch {}` blocks — ownership errors invisible, wrong relations used silently |
| BUG-082 | Medium | v2.5.0 | `NanobotSystem.Scanning.cs` swap sites | Won't Fix | Cross-collection consistency gap: weld/grind/float targets swapped under separate locks — theoretical one-tick mixed state. **Accepted as known limitation**: self-correcting (next tick re-aligns), no incorrect behavior (a stale entry just gets skipped via `NeedRepair`/`Closed`), microsecond-wide race window. Proposed unified-lock fix would serialize three currently-independent locks → measurable contention regression on the main thread. Profiling can't surface this (race timing is invisible in logs). Trade-off favors leaving finer-grained locking in place |
| BUG-083 | Low | v2.5.0 | `NanobotSystem.Scanning.cs:36-37` | Won't Fix | `_LastTargetsUpdate`/`_LastSourceUpdate` (TimeSpan, 8 bytes) read from game loop, written from background scans without memory barrier. **Accepted as known limitation**: target is x64 .NET 4.8 where aligned 8-byte reads/writes are atomic at the CPU level — torn reads can't be reproduced. Worst-case impact is "scan interval off by one cycle," smaller than natural scheduling jitter, invisible to players, no accumulating drift. Fix options would either add per-access `Volatile.Read`/`Volatile.Write` plumbing (maintenance cost, zero observable benefit) or `lock(_Welder)` on a per-frame timer check (measurable cost, no measurable benefit). Same shape as BUG-082 |
| BUG-084 | Low | v2.5.0 | `docs/pages/.../FAQ/index.md`, `Config/index.md` | Done | Documentation references removed `-cwsf` command — replaced with `/nanobars config create` |
| BUG-085 | High | v2.5.0 | `NanobotSystem.Update.cs` | Done | Fast mode (multiplier > 10) skips housekeeping: settings never save, friendly damage never cleaned, resource sink stale, state sync broken (v2.4.4) |
| FEAT-066 | High | v2.5.0 | Done | Decouple update frequency from multiplier — new `WorkSpeed` setting (1-10) controls operation tick rate independently. Fixes BUG-085 |

## Previously Fixed (not in this review)

These were identified and fixed on the `fix/v2.5.1` branch — see MEMORY for details:
- Idle BaRs when `MaxSystemsPerTargetGrid` limit fires (welding loop)
- Idle BaRs when `MaxSystemsPerTargetGrid` limit fires (grinding loop)

| BUG-086 | High | v2.5.1 | `NanobotSystem.Scanning.cs` | Done | Grind sort ignores user settings: per-grid cap overrides `GrindIgnorePriorityOrder`, `TruncateGridAware` disrupts sort, smallest-grid interleaves blocks from same-size grids |
| BUG-087 | Low | v2.5.1 | `Effects.cs` | TODO | Effects no distance culling |
| BUG-088 | High | v2.5.2 | `NanobotSystem.Scanning.cs` | Done | Cluster distant members starved on large single-grid bases — coordinator-centric collection cap and sort starve BaRs >150m from the coordinator; companion PB script shows `NULL` target |
| FEAT-067 | Medium | v2.5.2 | Done | `/nanobars version` chat command — prints client version, and on dedicated servers also the server version via a network roundtrip, so players can diagnose version mismatches |
| BUG-089 | Medium | v2.5.2 | `NanobotSystem.Operations.cs` | Done | Idle fast-path (FEAT-039) skips `ServerTryPushInventory` when BaR has leftover items but is not full — auto-push silently stops while idle; items stuck until next target acquired |
| BUG-090 | Low | v2.5.2 | `NanobotSystem.Scanning.cs:1681`, `Inventory.cs`, `NanobotSystem.cs` | Done | Push-target backoff (BUG-016) compares `Count` only — same-size container swaps leave `_PushTargetsFull` armed for up to 60s; replaced with count+EntityId-XOR signature |
| BUG-091 | Medium | v2.5.2 | `NanobotSystem.Scanning.cs` | Done | `GrindSmallestGridFirst` ordered same-size grids by arbitrary `EntityId` (creation order) instead of proximity — BaR could travel to a far equal-size grid before a nearby one. Per-grid min-distance lookup now drives the tiebreaker (follow-up to BUG-086) |
| BUG-092 | Medium | v2.5.2 | `NanobotSystem.Update.cs` (hot path) | Won't Fix | Fast mode (`WorkSpeed ≥ 5`) triggers periodic 100-170ms main-thread pauses on 50+ BaR clusters; stagger-throttled `UpdateBeforeSimulation10_100` captures what looks like gen2 GC pauses (can't confirm — `System.GC.*` is sandbox-prohibited). Default `WorkSpeed=1` is clean. **Closed**: original repro is from v2.5.2 and stale — substantial intervening allocation work (BUG-098, 110, 111, 117, 119) directly targeted the per-tick allocation patterns flagged as suspects. Workaround (keep `WorkSpeed=1` on servers with 10+ BaRs) already exists and matches defaults; players opting into `WorkSpeed ≥ 5` on large clusters explicitly trade sim-stability for speed. If a future fast-mode profile reproduces the same signature, file a new ticket with current data rather than re-opening |
| BUG-093 | High | v2.5.3 | `Models/SyncModSettings.cs` | Done | Empty `AllowedGrindJanitorRelations` in ModSettings.xml silently broke grinding on every BaR — `SyncBlockSettings.cs:772` AND-masks each block's `UseGrindJanitorOn` against the mod-wide value. Version-gated v5 migration didn't run on files already at v7. Unconditional clamp in `Load()` validation auto-heals broken configs on load |
| BUG-094 | High | v2.5.3 | `NanobotSystem.Scanning.cs` | Done | Farthest-first grind sort starts in the wrong place on grids larger than the scan cap — per-block count gate in `AsyncAddBlockIfTarget` short-circuited after 1024 candidates, leaving the per-grid sort operating on an arbitrary cache-order prefix instead of the true top-N. Regression from v2.5.0 deleting the full-grid pre-sort but keeping the cap gate. Fix passes `int.MaxValue` through the per-block gates and relies on post-loop `SortAndCapGridCandidates` |
| BUG-095 | Medium | v2.5.3 | `NanobotSystem.Scanning.cs` (`StartAsyncClusterScan` / `StartAsyncApplyClusterResults` early-exit) | Done | Disabled/unpowered early-exit unconditionally cleared `_AsyncUpdateSourcesAndTargetsRunning` without checking if a background scan from a prior tick was still in flight — a disable→enable bounce across ticks could enqueue a second concurrent scan of the same BaR, producing interleaved target-list swaps. Fix gates the cleanup on the flag under `lock(_Welder)`; if a scan is running the early-exit returns and lets the background finally clear the flag naturally |
| BUG-096 | High | v2.5.3 | `NanobotSystem.Scanning.cs` (`SortAndCapGridCandidates`, `AsyncAddBlocksOfGrid`) | Done | Two BUG-094 follow-ups: (A) farthest-first in multi-member cluster picked blocks nobody could reach because `skipRangeCheck=true` collection let out-of-range blocks into the sort input — fixed by partitioning in-range-to-any-member candidates before sorting; (B) cluster-wide per-type cap no longer enforced after `int.MaxValue` gate change, list grew unbounded across grids in grind-only/weld-only workloads — fixed by cap check at grid entry (nulls per-type list for that grid without touching BUG-094's per-block path) plus empty-grid-cache safety guard |
| BUG-097 | High | v2.5.3 | `NanobotSystem.Operations.cs`, `Handlers/PriorityHandling.cs`, `Models/SyncBlockState.cs` | Done | Three issues from the scan/sort/priority/work-mode audit: (A) `GrindIfWeldGetStuck` was functionally identical to `WeldBeforeGrind` — fall-through `!(welding \| transporting)` fired when there was simply nothing to weld; now requires `needWelding && !welding && !transporting` (weld actually stuck) — **superseded by BUG-101 (mode removed entirely after deadlock report)**; (B) `_PrioHash` had a data race between main-thread `UpdateHash` (Clear + Add) and background-scan sort comparator `TryGetValue` — fixed by atomic reference swap; (C) `CurrentWeldingBlock`/`CurrentGrindingBlock` setters did Dec→Inc on the same `GridSystemCount` bucket during same-grid lock-on reference refresh, briefly dipping the counter and letting other BaRs bypass `MaxSystemsPerTargetGrid` — fixed by skipping the pair when old and new grid IDs match |
| BUG-098 | Medium | v2.5.3 | `Handlers/BlockSystemAssigningHandler.cs`, `Handlers/SafeZoneHandler.cs`, `NanobotSystem.Operations.cs`, `Helpers/InventoryHelper.cs` | Done | Hot-path allocation cleanup from the audit. (A) `BlockSystemAssigningHandler.GetBlockKey` allocated a new `string` via `string.Format("{0}:{1}", ...)` on every call from the main-thread weld/grind loops — up to ~768 string allocations per `ServerTryWelding` call per BaR. Replaced with a `BlockKey` struct implementing `IEquatable<BlockKey>` so the `ConcurrentDictionary` lookup is zero-alloc. (B) Eight `MethodProfiler.StopAndLog(..., () => ...)` call sites in hot paths were missing the `if (profilerTs != 0L)` guard, so the Func+closure was allocated per call even with profiling disabled: SafeZoneHandler (3), `TryTransmitState` (3, main thread per tick per BaR), `AddIfConnectedToInventory` (2). All eight wrapped in the guard matching the established pattern |
| BUG-099 | Medium | v2.5.3 | `NanobotSystem.Scanning.cs` (`SortAndCapGridCandidates`) | Done | Profiling session `20260413214958` (58-member cluster, 11k-candidate mega-base grid) showed `SortAndCapGridCandidates` at 28.5 ms steady avg (max 125 ms, sim-speed dipped to 0.77 during big scans). Root cause: the sort comparator recomputed `min(squared-distance to 58 member centers)` for **both** blocks on **every comparison** — ~18M squared-distance calcs per 11k-item sort. `PreSortClusterCandidates` already avoided this via a dict cache; `SortAndCapGridCandidates` was missing the same optimization. Fix: populate `_sortCandidateDistances` (new pooled per-BaR field) during the BUG-096 partition pass (piggybacks on the existing per-candidate iteration), sort comparator does a `TryGetValue` per compare. **Measured** via re-profile session `20260413215939`: sort cost -69% (92 → 28.5 ms avg), total per call -64% (97 → 35 ms), method total -66% (1133 → 381 ms), total mod CPU per 120 s session -35% (12,985 → 8,469 ms), sim-speed min 0.77 → 0.80, main-thread max spike 15.6 → 12.9 ms |
| BUG-100 | Medium | v2.5.3 | `NanobotSystem.Scanning.cs` (`SortAndCapGridCandidates`) | Done | Follow-up to BUG-099. Profile session `20260413220505` (58 members, 4 large grids, 180 s, ~9,900 candidates per grid) showed sort still at 37-46 ms per call — BUG-099's distance cache removed the recompute cost but the comparator's `BlockGrindPriority.GetPriority` / `BlockWeldPriority.GetPriority` calls were the new bottleneck (~34 ms of priority lookups per 9.9k-candidate sort). Fix: pre-fetch priority once per candidate during the BUG-096 partition pass into a new `_sortCandidatePriorities` pooled dict, inline the `SortAndCapGridCandidates` comparator to read from the cache (shared helpers stay untouched). **Measured** via re-profile session `20260413222549`: `SortAndCapGridCandidates` -25% total (2,678 → 2,007 ms), -28% avg (46.2 → 33.4 ms), -37% steady avg (48.2 → 30.6 ms); cascade savings `AsyncClusterScan` -21.5% (4,049 → 3,177), `AsyncAddBlocksOfGrid` -21.5% (4,077 → 3,201); total mod CPU -12% (26,098 → 22,919 ms); **sim-speed min 0.68 → 0.80**, avg 0.96 → 0.99; main-thread max spikes all down ~30% (ServerTryWeldingGrindingCollecting 16.8 → 11.8 ms). Actual sort savings ~30% of predicted magnitude — dict lookup with interface-type keys slower than estimated — but user-visible sim metrics are the strong wins |
| BUG-101 | High | v2.5.4 | `NanobotSystem.Operations.cs`, `Models/SyncBlockSettings.cs`, `Terminal/ComboBoxes.cs`, `Terminal.cs`, `Models/SyncModSettings.cs`, `Models/SyncModSettingsWelder.cs` | Done | Player-reported regression from BUG-097 section A. The narrowed `weldStuck = needWelding && !welding && !transporting` gate left the BaR permanently in `State: Idle` whenever the weld loop had no targets or was exhausted-and-skipped — `needWelding` initialized `false` and only flipped on iterating an unweldable target, so the gate could never open. Resolved by **removing `GrindIfWeldGetStuck` entirely**: dropdown entry deleted, `WorkMode` setter and merge path silently rewrite the deprecated value to `WeldBeforeGrind`, switch case stacks defensively onto `WeldBeforeGrind`, default `AllowedWorkModes` no longer includes the bit, enum value `0x0004` reserved for backwards-compat deserialization. Mode was redundant with `WeldBeforeGrind` and the label was ambiguous; removal preferred over re-litigating the definition |
| BUG-102 | Medium | v2.5.4 | `NanobotSystem.Update.cs`, `Mod.cs` | Done | Per-BaR stagger formula `clusterSize < 6 ? 1 : ...` conflated isolated BaRs (`clusterSize == 1`, no shared scan) with small clusters (size 2-5, scan amortizes). Discovered via `thenebula` profile (2026-04-28, v2.5.3): 31 isolated BaRs all logged `effectiveGroups=1` and fired on the same tick. **Half A** (`Update.cs`): split the gate three ways — `clusterSize == 1` now uses `Mod.GetEffectiveStaggerGroupCount()` directly; small-cluster (`< 6`) and large-cluster paths unchanged. Also fixed the profiler log that recomputed `effectiveGroups` inline with an off-by-one (`< 5` vs `< 6`) and dropped the sim-penalty — log now reads the actual computed value via hoisted method-scope locals. **Half B** (`Mod.cs`): `GetEffectiveStaggerGroupCount` was counting only `IsWorking` BaRs, returning 1 when all BaRs were disabled — making Half A inert. Re-profile `thenebula2` (v2.5.4 Half A only, 31 disabled BaRs) confirmed: still `effectiveGroups=1`, zero `throttle=stagger` events. Half B counts placed BaRs (`NanobotSystems.Count`); disabled BaRs still run per-tick orchestration (`CleanupFriendlyDamage`, `Settings.TrySave`, `TryTransmitState`) so they generate per-tick CPU and need to count toward stagger sizing |
| BUG-103 | High | v2.5.4 | `NanobotSystem.Operations.cs`, `NanobotSystem.Grinding.cs` | Done | Player-reported: BaRs stuck in `Welding (Transporting)` state, target a block but never weld it. Root cause: the per-BaR transport timer (`State.CurrentTransportTime`, sized by `2 × distance / WeldTransportSpeed` ≈ 5-6 s for 100 m targets) was purely cosmetic (drives visual particle position in `Effects.cs:393`) but **two gates blocked actual weld/grind work** for its full duration. `Operations.cs:125` `if (!transporting)` wrapped the entire work-mode dispatch — skipped 3-4 work cycles per transport. `Grinding.cs:155` `if (transporting) return false;` short-circuited `ServerDoGrind`. Items are picked synchronously by `ServerFindMissingComponents` (welding) and immediately drained by `ServerEmptyTransportInventory` (grinding), so the timer wait was unnecessary. `thenebula4` profile (31 BaRs ON, 120 s) showed only 185 `ServerDoWeld` and 5 `ServerDoGrind` calls — duty cycle ~15-20 %. Both gates removed; weld/grind dispatch and `ServerDoGrind` now run every work cycle. The work-cycle gate (`Update.cs:122`) still caps to ~1.6 s per BaR (workSpeed=1), giving 3-4× throughput improvement. Visual particles unchanged — `State.Transporting` still reflects the timer for `Effects.cs`. Distance no longer rate-limits work; users wanting slower far-target work can lower `WeldTransportSpeed`/`GrindTransportSpeed` (now decorative, also slows visuals) or use `WeldingMultiplier`/`WorkSpeed` knobs |
| BUG-104 | Medium | v2.5.4 | `Helpers/DlcCheckHelper.cs` (deleted), `NanobotSystem.Scanning.cs`, `Mod.cs` | Done | DLC check filtered out projected DLC-tagged blocks when the BaR owner is offline. `DlcCheckHelper.GetOwnedDlcs` called `UtilsPlayer.GetPlayer(ownerId)` which only resolves currently-connected players (`MyAPIGateway.Players.GetPlayers` is online-only). Offline owner → `player==null` → empty owned-DLC set → `IsBlockDlcAvailableForOwner` returned false for every DLC-tagged block at `Scanning.cs:441,785`. Compounded by `_OwnerDlcCache` storing the empty set; logged-back-in owners stayed broken until the periodic `CleanupOwnerCache()` TTL fired. Net effect: BaRs couldn't build DLC blocks while owner offline — opposite of the autonomous-build use case. Fix: dropped the check entirely (option A). Projector already enforces DLC at projection-time, so a DLC-tagged projected block is by definition sanctioned to be built — the BaR-side recheck duplicated upstream enforcement and had no real value. Removed both call sites in `Scanning.cs`, removed the periodic `CleanupOwnerCache()` invocation in `Mod.cs:432`, and deleted `Helpers/DlcCheckHelper.cs` (no remaining consumers). Independent of BUG-103 (bypassing the check during thenebula triage didn't fix the welding issue) |
| BUG-105 | Medium | v2.5.4 | `NanobotSystem.Grinding.cs`, `NanobotSystem.Welding.cs` | Done | Diagnostic instrumentation. `20260428200417-profiling` showed a 19.6 ms `ServerDoGrind` with only 0.4 ms profiled. Added `tsFriendly` + `friendlyIter`, `tsMechCheck`, `tsDeform`, `tsResolve` plus early-exit log entries. **Re-profile `20260428202503-profiling` ruled out the friendly-damage hypothesis** — `friendlyMs` total was 31 ms across 427 grinds (avg 0.073 ms, max 0.791 ms), `friendlyIter=58` confirmed but each `GetUserRelationToOwner` is ~1 µs not 0.3 ms. Real bottlenecks surfaced: **`decreaseMs` (5-12 ms on full-dismount armor)** and **`buildMs` (7-9 ms on `proj.Build`)** — both SE engine cascade costs. Original 19 ms spike was likely a one-off GC pause; histogram in re-profile shows 99.5% of grinds <2 ms, 0 spikes ≥20 ms. Hidden cost found: `tsResolve` 2.5 ms on `SmallHydrogenThrust` post-build resolution. Follow-ups: **BUG-106** (dismount budget), **BUG-107** (proj.Build budget), **BUG-108** (split tsResolve to find which sub-step dominates) |
| BUG-106 | Medium | v2.5.4 | `Mod.cs`, `NanobotSystem.Grinding.cs` | Done | Global per-tick budget for full-dismount grinds. BUG-105 instrumentation surfaced `decreaseMs` (5-12 ms on armor-block full dismount) as the dominant grind cost — SE engine cascade for grid integrity recalc, conveyor refresh, block-removal events. Cost is unavoidable per-call but compounds when N BaRs converge: 8 BaRs simultaneously dismounting = 8 × 12 ms = 96 ms tick stall. Mirrored the existing `TryClaimMechanicalGrindSlot()` (1/tick for mechanical blocks): added `Mod.TryClaimDismountSlot()` (cap 3/tick) and gated it in `ServerDoGrind` **before** `DecreaseMountLevel` when `integrityRatio <= 0` predicts full dismount. Mechanical-slot cap remains as a stricter sub-budget; mechanical blocks must claim both. Skipped grinds stay in `PossibleGrindTargets` and retry next tick. Logs `earlyExit=dismountSlot` when slot exhausted |
| BUG-107 | Medium | v2.5.4 | `Mod.cs`, `NanobotSystem.Welding.cs` | Done | Global per-tick budget for `proj.Build()` calls. BUG-105 instrumentation surfaced `buildMs` (7-9 ms on projected armor/conveyor blocks) as the dominant projected-weld cost — SE engine materialization + grid topology update. Same compounding risk as BUG-106 (multiple BaRs scaffolding a freshly-projected blueprint). Added `Mod.TryClaimProjBuildSlot()` (cap 3/tick) and gated it in `ServerDoWeld` before `proj.Build()`. **Skips both build AND the post-build resolve** when slot exhausted — calling `GetCubeBlock` on a not-yet-built block would null the target and set `Ignore=true`. Returning `false` keeps the projected block in `PossibleWeldTargets` for next-tick retry. Logs `earlyExit=projBuildSlot` |
| BUG-108 | Low | v2.5.4 | `NanobotSystem.Welding.cs` | Done | Diagnostic split of `tsResolve` delivered (`resolveCoordMs` + `resolveLookupMs` sub-timers, lines 600-606 / 728). Original symptom was a single 2.5 ms outlier on `SmallBlockSmallHydrogenThrust` post-`proj.Build` projected→physical resolution from session `20260428202503-profiling` — **1 occurrence in 649 ServerDoWeld calls (0.15%)**, amortized ~3.7 µs/call. Severity Low at filing and nothing has raised it. Latest profile (`20260429145732-profiling`) had zero `ServerDoWeld` calls so no new data on which sub-step dominates. Closed as **diagnostic complete** following BUG-105/113 precedent — the instrumentation is permanent and self-monitoring; future projected welds will automatically populate the per-sub-step breakdown. Why no fix even with data: caching `GetCubeBlock` results gets zero hits (each projected block resolves once before becoming physical), caching the per-projector coordinate transform adds invalidation logic for microsecond savings against a 0.15% rate, and surrounding logic was already flagged "unlikely" in the ticket. Re-open with new session ID + captured `resolveCoordMs`/`resolveLookupMs` data if the resolve path becomes a recurring multi-ms cost in a future profile |
| BUG-109 | Medium | v2.5.4 | `NanobotSystem.Scanning.cs` | Done | Audit of `Scanning.cs` (2,403 LOC, 32 `new` calls) for the recurring 21→70% CPU spike rhythm during grinding. Identified 7 critical per-scan list/dict allocations, 4 per-block/candidate allocations (3 of them confirmed reference types — `ClusterTargetCandidate`, `ClusterFloatingCandidate`, `TargetBlockData` — all `class`), and 11 lambda-closure sites in Sort/RemoveAll comparators. Audit correction: `MyOrientedBoundingBoxD` (lines 1808, 1094, 1123) is a VRageMath struct so its `new ...()` is stack-allocated, not heap. **No LINQ, no boxing, no missing profiler closure guards** — instrumentation hygiene is clean. Initial plan to add `GC.GetTotalMemory(false)` sampling abandoned: **`System.GC` is on the SE sandbox prohibited list** (per CLAUDE.md, same family as `System.Threading`/`System.Reflection`). Diagnostic complete; pooling work moved to BUG-110. Lambda closures and class-to-struct deferred to BUG-111 |
| BUG-110 | Medium | v2.5.4 | `NanobotSystem.cs`, `NanobotSystem.Scanning.cs` | Done | First pass of allocation pooling on the cluster-scan thread, mirroring the existing `_ClusterMemberAreaCenters` lazy-init + Clear pattern. Added 7 new instance fields: `_ScanGridsBuffer`, `_ScanSourcesBuffer`, `_ScanPreSortDistances` for `AsyncClusterScan`/`PreSortClusterCandidates`; `_ScanSourceVisitedGridIds`, `_ScanSourceGridQueue` for `AsyncScanForSources`; `_ScanSortedGrids`, `_ScanNonGridEntities` for `AsyncAddBlocksOfBox` (`GrindSmallestGridFirst` path). Each call site now does `if (field == null) field = new ...; else field.Clear();` and aliases the pool field to the existing local var name so method bodies are unchanged. Background scans are serialized per BaR via `Mod.AddAsyncAction()`, so no locks needed on the pool fields. Eliminates 5-7 heap allocations per cluster scan (plus the corresponding internal-array growth as the lists fill). **Re-profile `20260428212757`**: AsyncClusterScan avg 200→116 ms (-42%), AsyncAddBlocksOfBox avg 182→110 ms (-40%), AsyncAddBlocksOfGrid avg 7.1→4.5 ms (-37%), ServerDoGrind max 12.5→3.8 ms (-70%). Sim-speed avg 1.00 with 4× heavier scan workload. Spike rhythm reduced but still visible early in session — BUG-111 follow-up |
| BUG-111 | Medium | v2.5.4 | `Cluster/ScanClusterResult.cs` | Done | Convert `ClusterTargetCandidate` and `ClusterFloatingCandidate` from `class` to `struct`. After BUG-110 pooled the per-scan collections, the dominant remaining GC source was 100-1000 per-candidate `new ClusterTargetCandidate(...)` and `new ClusterFloatingCandidate(...)` heap allocations per cluster scan (sites: `Scanning.cs:455,476,551,798,975,985,995`). Both types are small (2 fields, no inheritance, no virtual methods, mutated only at construction) — textbook struct candidates. Audited for in-place-mutation patterns (`list[i].Field = ...` or `var x = list[i]; x.Field = ...`) — none found; all references outside the constructor are reads. `QuickSelect`/`Sort` use whole-element swap (`list[i] = list[j]`) which works identically for structs. `IComparer<T>` paths compare by value, no boxing. Field layouts + constructor signatures unchanged so all callers continue to compile. Eliminates ~1000 heap allocations per cluster scan (~8,000-12,000/min on busy servers). Backing-array growth in `List<T>` still allocates but at `O(log N)`. `ScanClusterResult` itself stays a class (publish-via-reference-swap pattern requires reference identity) |
| BUG-112 | Medium | v2.5.4 | `NanobotSystem.Scanning.cs` | Done | `AsyncAddBlockIfWeldTarget` non-projected branch had `block.NeedRepair(Settings.WeldOptions)` as the **last** condition in the && chain. Due to short-circuit evaluation this ran color compares (~100 ns), priority dict lookup (~100 ns), and **`IsRelationAllowed4Welding(block)` (engine call, 1-10 µs)** before checking whether the block actually needed repair. For a stable 11k-block base where ~99% of blocks are full integrity, the relation lookup ran on every block that was going to bail anyway. Profile `20260428222525-profiling` showed `AsyncAddBlocksOfGrid` at 219-237 ms on an 11k-block grid; per-block cost was ~22 µs with most of it in the relation lookup. **Fix**: reorder so `NeedRepair` runs first — full-integrity blocks bail in ~50-200 ns, skipping the engine relation lookup. Set of qualifying candidates is unchanged (`&&` is commutative for predicate truth, only evaluation order differs); side-effect of `NeedRepair` (`ResetSkeleton` on deformed full-integrity blocks) reaches the same set of blocks as before. Estimated saving: ~30-35 ms per 11k-block scan when most blocks are stable. Grinding path (`AsyncAddBlockIfGrindTarget`) audited too — already runs `BlockGrindPriority.GetEnabled` short-circuit before relation lookup, no reorder needed. The pre-existing TODO at `NanobotSystem.State.cs:107` ("relation lookup is called per-block per-scan, consider caching per-grid") remains as future work for the remaining ~1% damaged-block path |
| FEAT-068 | Medium | v2.5.2 | Done | Companion script — per-LCD config: `[BaR:group]` name tag with multi-surface `@0,1,2`, unscoped + scoped `@BaR@<surfaceIdx>` Custom Data blocks, `FontSize=<n>/auto` key, forced Monospace font for column alignment, auto-seeded default template in empty Custom Data, `Auto-queuing` state line on the Status page, reinit interval 120 s → 30 s, and script comment cleanup (2634 → 2075 lines) |
| FEAT-069 | Medium | — | Proposed | Public ModAPI (delegate-dictionary via `SendModMessage`) so other mods can read and seed BaR settings from code — `TryInitializeSetting` honors admin `ModSettings.xml` precedence, `SetSetting` is unconditional. Extracts shared `SettingsRegistry` from `ConfigCommand` and exposes the 4 `MaximumRequiredElectricPower*` settings that are currently only tunable via direct code |
| FEAT-070 | Medium | v2.5.3 | Done | Consolidate the seven open-coded weld/grind sort comparators in `NanobotSystem.Scanning.cs` into three shared helpers (`CompareGrindNonDistance`, `CompareWeldPriority`, `CompareBlockStableTiebreak`). Every call site (SortAndCapGridCandidates, PreSortClusterCandidates × 2, ApplyClusterResultToSelf × 4) now delegates the non-distance key while keeping local control of distance metric and direction. Folded behavior fix: `ApplyClusterResultToSelf`'s two grind sort sites previously used unconditional nearest-first within same-size grids after the smallest-grid-first tiebreakers, ignoring the user's `GrindNearFirst` flag — now consistent with `PreSortClusterCandidates` so farthest-first is honored end-to-end |
| FEAT-071 | High | v2.5.4 | In Progress | Idle scan backoff — when cluster coordinator finds zero targets for 3 consecutive scans (~30s), extend scan interval from 10s to 30s. Resets instantly when targets appear. Profiling shows 582ms/180s of background scanning producing zero results on 58 idle BaRs |
| FEAT-072 | Medium | v2.5.4 | In Progress | Dirty-flag cluster key optimization — replace per-second `ComputeClusterKey()` string recomputation (0.586ms avg × 171 calls = 103ms/180s) with a version counter that triggers rebuild only on actual settings changes. Eliminates 12+ string concatenations per BaR per second in idle fast path |
| FEAT-073 | Medium | v2.5.4 | In Progress | Empty grid connection traversal optimization — in the empty-grid-cache fast path of `AsyncAddBlocksOfGrid`, iterate only fat blocks instead of all blocks (skips hundreds of armor blocks on large grids). Connection blocks (mechanical, connector, attachable) are always fat blocks |
| FEAT-074 | High | v2.5.4 | In Progress | Quickselect sort optimization — replace O(n log n) full sort in `SortAndCapGridCandidates` with O(n) quickselect + O(k log k) partial sort. On an 11,732-block grid (profiling session `20260416205112`) the sort step drops from 20-33ms to ~3ms per scan. Correctly handles nearest/farthest ordering since all candidates are still considered |
| FEAT-075 | High | v2.5.4 | In Progress | Saturated scan skip — skip full-grid rescan when coordinator has >64 live targets. Per-type saturation check for mixed work modes (grind+weld). Forced rescan with 5s debounce when members deplete targets. Coordinator exhaustion bypass for stale projected-block references. Idle counter now uses coordinator's filtered targets (not cluster-wide raw count). Profiled: scans reduced from 17 to 3-7 per 180s |
| FEAT-076 | Medium | v2.5.4 | In Progress | Grind loop exhaustion — when `ServerTryGrinding` finds nothing grindable (all grid-limited/assigned/destroyed), skip the 256-entry iteration on subsequent ticks. Resets on target list hash change or saturated grid set change. Falls through correctly to welding in GrindBeforeWeld mode |
| FEAT-077 | Medium | v2.5.4 | In Progress | Projector cold-start detection — `HasBuildableProjectorOnGrid()` checks `BuildableBlocksCount > 0` on projectors across own grid, connected grids, and BoundingBox entities during idle. Triggers immediate scan within 1-2s of a player placing the first block of a projection. Respects WorkMode (skipped for GrindOnly) |
| FEAT-078 | Low | v2.5.4 | In Progress | Profiler guard fixes — add `if (profilerTs != 0L)` guards to 16 unguarded `StopAndLog` calls across 10 files. Eliminates ~140 garbage lambda closure allocations/second when profiling is disabled. New diagnostic profiling points: `CheckAndUpdateInventoryFull`, `RebuildSaturatedGrids`, `ImmediateRescanTrigger`, `CleanupFriendlyDamage` |
| FEAT-079 | Low | v2.5.4 | In Progress | Custom info panel — show "Next target scan: Xs" when idle (server), "Scanning for targets..." on clients. Debug mode: scan mode, idle count, last scan candidate counts, forced flag. Helps players understand the BaR is waiting for targets, not stuck |
| FEAT-260511.1 | Medium | v2.5.4 | `Mod.cs`, `Managers/PerTickBudget.cs`, `Models/SyncModSettings.cs`, `Chat/Commands/ConfigCommand.cs` | Done | Configurable per-tick ms budgets for weld / grind work. Previously hard-coded `MaxGrindMsPerTickDefault = 8` / `MaxWeldMsPerTickDefault = 8`; with many BaRs (≥30) the ms cap typically wins over the count cap and admins saw only ~16 BaRs visibly grinding while the rest sat idle. Added `MaxGrindMsPerTick` / `MaxWeldMsPerTick` settings (range 1-100, default 8) with `Func<double>` resolver on `PerTickBudget` so the cap can be re-tuned at runtime via `/nanobars config set …`. Recommended start for fleets > 30 BaRs: 30 ms each |
| FEAT-260511.2 | Low | v2.5.4 | `Chat/ChatHandler.cs`, `Handlers/HudHandler.cs`, `NanobotSystem.Update.cs`, `Cluster/ScanClusterCoordinator.cs` | Done | `/nanobars debug cluster-area` toggle — local wireframe overlay of every multi-member cluster's member working areas, drawn with `BlendTypeEnum.PostPP` so visible through walls. 8-colour palette (yellow/pink/green/purple/cyan/orange/red/white) keyed by cluster-key hash; coordinator marked by a tall green pillar above its block. On toggle-on, chat prints a per-cluster size summary labelled by colour (`pink=20, yellow=12, ...`). New `ScanClusterCoordinator.IsClusterEligible(NanobotSystem)` helper is single source of truth for cluster membership filter; called from server `RebuildClusters` and client visualization. Strict `!IsDedicated` guard — zero cost on a DS |
| FEAT-260511.3 | Low | v2.5.4 | `Chat/ChatHandler.cs`, `Handlers/HudHandler.cs`, `Handlers/BlockSystemAssigningHandler.cs`, `NanobotSystem.Update.cs` | Done | `/nanobars debug targets` toggle — red wireframes around every BaR's current weld/grind target blocks. Border colour = cluster of the BaR that discovered the target; solid red interior added when block is currently assigned via `BlockSystemAssigningHandler`. Unassigned = wireframe only; assigned = wireframe + solid red. New `IsLiveWeldTarget` / `IsLiveGrindTarget` filters skip stale list entries (welded-to-full, destroyed, closed). Per-frame `HashSet<BlockKey>` dedup so a block targeted by N BaRs renders once. New `BlockSystemAssigningHandler.TryGetAssignedSystem` extension. Strict `!IsDedicated` guard |
| FEAT-260511.4 | Medium | v2.5.4 | `NanobotSystem.Scanning.Block.cs` | Done | Union-AABB cluster discovery. `AsyncAddBlocksOfBox` used to call `SharedEntityCache.GetEntitiesInBox` with only the coordinator's AABB, so members positioned outside that box couldn't see external target grids. Now unions the AABBs of every cluster member (already snapshotted into `_ClusterMemberAreaBoxes` for the existing per-member sort path) before the query. Per-block range filter downstream still uses each member's own OBB in `ApplyClusterResultToSelf`, so coverage semantics are unchanged — only which grids get a chance to be considered. Trade-off: bigger query → 30-65 ms `AsyncAddBlocksOfBox` on a 58-member cluster (background thread). Solo clusters (memberCount ≤ 1) skip the union and behave exactly as before. Profiler line adds `clusterUnion=True/False;memberBoxes=N` |
| FEAT-260511.5 | Medium | v2.5.4 | `NanobotSystem.Init.cs`, `Handlers/BlockSystemAssigningHandler.cs` | Done | Release every claimed block when a sort-relevant flag toggles. FEAT-080 already triggered a rescan; this extends it so BaRs *also* drop their in-flight claims when toggling Nearest/Farthest/Smallest-Grid/priority lists/etc. — the new sorted list takes effect at once instead of BaRs continuing on the old top blocks until completion. Compares `ScanClusterCoordinator.ComputeClusterKeyHash` before/after; only fires when the hash actually changes, so cosmetic toggles (sound, ShowArea, multipliers) don't churn in-flight work. New `BlockSystemAssigningHandler.ReleaseAllForSystem(long systemId)` helper walks the TTL cache once and drops every entry for the given BaR — needed because the multi-action wrapper (FEAT-260511.6) could have assigned several blocks the BaR never settled on |
| FEAT-260511.7 | Low | v2.5.4 | `Chat/Commands/ConfigCommand.cs`, `Models/SyncModSettings.cs` | Done | `--global` flag for `/nanobars config save` (and alias `config create`) writes `ModSettings.xml` to PC-wide local storage (`%AppData%\SpaceEngineers\Storage\<mod>`) instead of the world folder, restoring legacy `-cpsf` parity. World file always wins on load (existing `SyncModSettings.Load` behavior). Also scoped `/nanobars config delete`: default now deletes the **world** file only; `--global` deletes the PC-wide file only; `--all` deletes both (previous behavior). `/nanobars config reload` now reports which source it actually loaded from (world / PC-wide / defaults) via new `SyncModSettings.LastLoadSource` (`SettingsLoadSource` enum). Net change is two arg-parsed branches in `ExecuteSave` / `ExecuteDelete` plus reload-source bookkeeping in `Load()` — underlying `SyncModSettings.Save(settings, world)` already supported both targets |
| FEAT-260511.6 | Medium | v2.5.4 | `NanobotSystem.Operations.cs` | Done (disabled) | Multi-action wrapper for `ServerTryWelding` / `ServerTryGrinding`. Loop inside `MultiWeld` / `MultiGrind` lets a single BaR claim multiple per-tick budget slots when its target list has them, instead of just one weld/grind per Update10. State.Current\*Block committed between iterations so `IsGridOverSystemLimit` sees fresh counts. **Currently set to `MaxActionsPerCycle = 1`** (single action per cycle) because cross-grid hopping silently undercounts `MaxSystemsPerTargetGrid` — a BaR that works grid G in iter 1 and grid H in iter 2 leaves G undercounted by the time the cycle ends. Wrapper structure is kept so re-enabling is a one-line change once per-cycle "grids touched" tracking lands as a follow-up |
| FEAT-080 | Low | v2.5.4 | `NanobotSystem.Init.cs`, `NanobotSystem.Operations.cs`, `NanobotSystem.Scanning.cs` | Done | Trigger an immediate rescan when terminal settings change. Player-facing: edit Work Mode / priority list / Area Size / colour filter etc. and see the BaR pick up matching targets right away instead of waiting up to 10 s for the next scan tick (FEAT-071 idle backoff was making the wait especially visible). Extracted the existing rescan-trigger logic from `Operations.cs:217-240` into `internal void TriggerImmediateRescan(string reason)` on `NanobotSystem.Scanning.cs`; called from the existing work-complete path (now logs `reason=weldComplete`/`grindComplete`) and from `NanobotSystem.SettingsChanged` after `UpdateCustomInfo` (logs `reason=settingsChanged`). Server-only via `MyAPIGateway.Session.IsServer` early-return. `_rescanForced` on the cluster coordinator bypasses the FEAT-075 saturated-skip so members get fresh data. Dispatch is serialized per-BaR via `Mod.AddAsyncAction`, so rapid slider drags can't pile up scans. Profiler `ImmediateRescanTrigger` log replaces the old `wasWelding`/`wasGrinding` fields with a richer `reason=` label |
| BUG-113 | Low | v2.5.4 | `NanobotSystem.Welding.cs` | Done | Diagnostic instrumentation. Previous BUG-105/108 covered `proj.Build`, resolve, stockpile, mount, deform. Several engine/handler calls inside `ServerDoWeld` were still uninstrumented: `_TransportInventory.FindItem`, `_Welder.IsWithinWorldLimits`, `target.CanContinueBuild`, `IsWeldIntegrityReached`, and the `BlockSystemAssigningHandler` `ReleaseFromSystem`+`AssignToSystem` swap. Added five new sub-timers: `tsFindItem`, `tsLimitsCheck`, `tsCanContinue`, `tsIntegrityCheck` (sums both call sites), `tsAssign` (sums both ops). Profile log fields added: `findItemMs`, `limitsMs`, `canContinueMs`, `integrityCheckMs`, `assignMs`. **Result confirmed via re-profile `20260428232214`**: `proj.Build` is 99% of every `ServerDoWeld` spike (e.g. 2.474 ms of a 2.491 ms cockpit build), all other sub-timers including the new ones are sub-10 µs. Welding spikes are SE engine cost, not BaR-mod cost. Instrumentation stays permanent — if a future spike shows non-buildMs dominance, the fields will identify it instantly |
| BUG-114 | Medium | v2.5.4 | `NanobotSystem.Inventory.cs` | Done | Player-facing semantics: when all three `PushIngotOreImmediately` / `PushComponentImmediately` / `PushItemsImmediately` flags were OFF, the welder would still dump items into push-target containers via the safety overflow path inside `ServerEmptyTransportInventory(push: true)` at `Inventory.cs:152-169`. The user-facing `ServerTryPushInventory` (`Inventory.cs:42-43`) was correctly gated by the flag mask and per-item-type checks, but the overflow safety branch (called from grinding, collecting, the operations cleanup, and the in-flight transport check) skipped the gate entirely and called `welderInventory.PushComponents(_PossiblePushTargets, null)` with no item-type filter whenever the welder was about to overflow. **Fix**: added the same flag-mask check (`Settings.Flags & (PushIngotOre + PushComponent + PushItems) != 0`) to the line-152 condition. When all three flags are off, the safety push is now skipped — welder fills, `CheckAndUpdateInventoryFull` (`Operations.cs:96`) trips `State.InventoryFull`, grind/collect paths halt until the player makes room or re-enables a flag. Welding path unchanged (it calls `ServerEmptyTransportInventory(false)` so this branch never ran for welds). Internal `_TransportInventory` → welder transfer at lines 171-189 stays unconditional |
| BUG-120 | Medium | v2.5.4 | `NanobotSystem.cs`, `NanobotSystem.Welding.cs`, `NanobotSystem.Init.cs` | Done | Player report: BaR keeps re-trying a projected DLC block for an online owner who lacks the DLC. `proj.Build()` returns cleanly without throwing (no BUG-115 NRE), but the physical block doesn't materialize — `projectorGrid.GetCubeBlock(blockPos)` returns null at `Welding.cs:626`. Existing code only set `targetData.Ignore = true` on the per-`TargetBlockData` instance, which gets wiped every ~2 s by background scan refresh, so the BaR re-feeds the same projected block to the weld loop every tick forever. **Fix**: extend BUG-115's persistent skip pattern to the silent-fail branch with two additions: (1) retry tolerance via new `_ProjBuildSilentFailCount` `Dictionary<string, int>` (3 consecutive ticks before promotion to `_BrokenProjBuildKeys`) so transient races don't immediately mark blocks broken; (2) **owner-scope invalidation** via new `_BrokenCacheOwnerId` `long` snapshot — `EnsureBrokenCacheOwnerScope()` helper called once at top of `ServerTryWelding` clears both caches when `_Welder.OwnerId` differs from the snapshot (new owner may have different DLC entitlements). Polling-based change detection (one `long` compare per tick); explorer agent confirmed codebase has no event-driven ownership detection. Player power-cycle reset added by extending the existing `_onEnabledChanged` lambda at `Init.cs:119-122` — toggling BaR off/on clears both caches as the natural UX trigger for "I just acquired DLC, retry now." **DLC pre-check (BUG-104) NOT restored**: `MyAPIGateway.DLC.HasDLC` queries SE's local entitlement cache which is unpopulated for offline players (returns false even when they own DLC) and on dedicated servers without an active Steam session — would reject more valid welds than DLC-missing ones. Build clean (0 warnings, 0 errors). Recovery paths: acquire DLC + power-cycle, transfer ownership to faction-mate with DLC, world reload |
| BUG-119 | Medium | v2.5.4 | `Helpers/InventoryHelper.cs`, `NanobotSystem.cs`, `NanobotSystem.Scanning.cs` | Done | Profile `20260429013527-profiling`: 2 177 calls × 0.80 ms avg = **1 738 ms** = 83% of all Inventory-domain time, with steady-state 23× slower than warmup (0.806 vs 0.035 ms). Reading the code revealed the doc's "no memoization" hypothesis was wrong — a `ConnectionCache` (`(blockId, welderId) → bool`, TTL 15 s) already existed. Real cost was on **both** paths. (1) Cache **miss path**: TTL 15 s vs ~6 s scan cadence → many entries expired between scans → engine `IsConnectedTo` conveyor walk fires repeatedly. (2) Cache **hit path**: still iterated `terminalBlock.GetInventory(i)` and ran `possibleSources.Contains(inventory)` — an O(n) linear scan against a list that grew to 50+ entries on large ships, exactly producing the warmup→steady-state asymmetry. **Fix** (two parts): bumped `ConnectionCache` TTL 15 → 60 s in `InventoryHelper.cs` (stale entries harmless — a disconnected inventory just fails its actual transfer in `PullComponents`, a newly connected one is picked up on the next refresh). Added reusable `_ScanSourceDedupSet` (`HashSet<IMyInventory>`) field on `NanobotSystem`; `AsyncScanForSources` clears and seeds it at scan start, passes it through. `AddIfConnectedToInventory` now uses `possibleSourcesSet.Add(inventory)` (returns false if already present, O(1)) instead of `possibleSources.Contains(inventory)` on both hit and miss paths; null-set fallback keeps the helper safe for future callers that don't supply one. Cache-miss path speculatively adds and rolls back via `Remove` if `IsConnectedTo` rejects. No grid-topology event hook needed — TTL extension covers it without subscribing globally. Expected: total drops from 1 740 ms toward 300-500 ms at the same call volume; secondary benefit to BUG-117/118 outer scans |
| BUG-118 | Medium | v2.5.4 | `NanobotSystem.Scanning.cs` `AsyncScanForSources` (no direct change) | Done | Original capture (`20260429013527-profiling`): 19 calls × 106.7 ms avg / **302.9 ms max** = 2 028 ms total. The ticket suggested investigating outer-loop work, but verification proved the inner predicate `AddIfConnectedToInventory` fully accounted for the cost — there was no additional outer scan work to optimize. **Fixed as a cascade of BUG-119** (TTL 15→60 s + HashSet dedup in the predicate); `AsyncScanForSources` itself only changed to wire the reusable `_ScanSourceDedupSet` through to the predicate. **Verification (`20260429144151-profiling`, sim-speed avg 1.00 / min 0.88, recovered from 0.69/0.43)**: AsyncScanForSources total **2 028 → 30.6 ms (98.5% ↓)**, max **303 → 16.8 ms (18× ↓)**, avg 107 → 10.2 ms. AddIfConnectedToInventory total 1 738 → 22 ms (98.7% ↓), steady avg 0.806 → 0.062 ms (13× ↓), max 14.9 → 1.98 ms. Inventory domain total 2 086 → 333 ms (84% ↓). Per-call log shows 2 grids visited / 94 sources found at 2-17 ms — genuine grid traversal cost only, no further optimization warranted. Cache hit rate 33% in verification session, limited by only 3 scans firing in 120 s; longer sessions would see higher hit rate. Side observation: AsyncClusterScan/AsyncAddBlocksOfBox are now the dominant background cost (BUG-117) — recommend re-profiling specifically before acting since this scenario differs from the original BUG-117 capture |
| BUG-117 | Medium | v2.5.4 | `Utils/Utils.cs` `GetUserRelationToOwner` | Done | Investigation on `20260429144151-profiling` showed AsyncClusterScan **17 × 454 ms avg / 537 ms max = 7 723 ms** background-thread total. Per-grid log identified 3 huge grids (~7 300 blocks each) being fully re-iterated every cluster scan; each grid contributes 128 grind targets (per-grid cap), 0 weld, but ~22 000 blocks total are re-evaluated every ~6 s. Per-block ~18-25 µs dominated by uncached engine calls; biggest avoidable single waste was `fatBlock.GetUserRelationToOwner` firing fresh on every block on every scan even when the block has no individual owner (most fat blocks inherit grid ownership — engine returns NoOwnership and our wrapper falls back to the cached grid relation anyway). **Fix (Layer A)**: added `OwnerId == 0` shortcut to `Utils.GetUserRelationToOwner` so blocks without individual ownership skip the engine call and go straight to `GridOwnershipCacheHandler.GetRelationBetweenGridAndPlayer` (cached). Semantically identical (engine returns NoOwnership in this case anyway), benefits every caller of the extension, not just the scan loop, no new state. **Verified `20260429145732-profiling`** (same scenario shape, sim 1.00 avg / 0.85 min): AsyncClusterScan total **7 723 → 4 953 ms (-36%)**, avg 454 → 354 ms (-22%), max 537 → 448 ms (-17%); AsyncAddBlocksOfGrid total **7 682 → 4 894 ms (-36%)**, avg 17.0 → 10.3 ms (-41%); 3-huge-grid per-scan min **135 → 83.9 ms (-38%)** (cleanest measurement, lowest contention noise), avg 150 → 113 ms (-24%); background-thread wall-clock cost 6.4% → 4.1%. Top of predicted 10-30% range — actually 36-41% headline reductions. No behavioral regression: same grid selection, same target counts, autograb still applies correctly. Layers B/C (per-grid candidate memoization, background-thread pre-filter cache) deferred — sim-speed unaffected and staleness max ~0.45 s is acceptable; staleness/invalidation complexity isn't justified by residual cost |
| BUG-116 | Medium | v2.5.4 | `NanobotSystem.Welding.cs` | Done | Profile session `20260429013527-profiling` (120 s, large-ship welding scenario) showed `ServerTryWelding` max **45.5 ms** with consistent spike signature `weldChecked=70-110; componentFails=3; starvedSkip=66-108`. Translation: when components are missing, the loop iterated 100+ candidate blocks and called `Weldable()` (engine `target.CanBuild(false)` or `NeedRepair/IsFriendlyDamage`, ~0.05-0.1 ms each) on EVERY block before the starved-priority skip filtered them out — because the starved-skip lived *inside* the `if (isWeldable)` branch at `Welding.cs:127-136`. Each starved block thus paid the full `Weldable` engine-call cost before getting cheaply rejected. **Fix**: added a cheap pre-filter ABOVE `Weldable()` that does the same `BlockWeldPriority.GetPriority` lookup (dict read, ~5 µs) and the same `starvedPriorityBits & (1L << priority)` check, then `continue`s before the engine call. Same blocks skipped, same counters incremented, just earlier in the iteration. Original post-Weldable skip kept as a fallback for the same-tick race where a priority gets newly starved after the current block has passed the pre-filter. Lock-on / lookingForNext / ScriptControlled paths preserved (those bypass the pre-filter via `!isLockOnBlock && !lookingForNext`). Expected: 10× reduction on spike path, max 45→~5 ms |
| BUG-121 | High | v2.5.5 | `NanobotSystem.Update.cs` | Done | Diagnostic instrumentation: added `tsPeriodic` / `tsResourceSink` / `tsSettingsSave` / `tsMsgSend` sub-timers to `UpdateBeforeSimulation10_100` to localize 59 ms / 49 ms wrapper spikes. **Re-profile `20260429181044`** ruled out all four segments — observed maxes 2.154 / 0.006 / 0.011 / 0.000 ms respectively. The wrapper's true spike trail (35 ms) was traced **inside** `ServerTryWeldingGrindingCollecting` → `ServerTryWelding` (27 ms): `ServerFindMissingComponents` 9.075 ms + `ServerDoWeld` 3.105 ms + ~14.85 ms unaccounted in the loop body. Welding-domain spikes only surfaced in this session (previous `20260429145732` was grind-only, ServerTryWelding=0.011 ms max). Secondary mystery: 14.18 ms `throttle=stagger` sample with all four new sub-timers zero — narrows to the unprofiled cycle/stagger calculation block at lines 90-118 or a GC pause. Followups: BUG-122 (loop body + `ServerFindMissingComponents` internals), BUG-123 (friendly-damage cache for the persisting 4.81 ms `friendlyMs` outliers in this session) |
| BUG-122 | High | v2.5.5 | `NanobotSystem.Welding.cs`, `NanobotSystem.Inventory.cs` | TODO | `ServerTryWelding` loop body has ~15 ms unaccounted cost; `ServerFindMissingComponents` projected-block 9-10 ms spike has no internal sub-timing. From BUG-121's diagnostic chain: top wrapper spike (35 ms) was traced into ServerTryWelding (27 ms) where ServerFindMissingComponents (9 ms, projected `SmallHydrogenThrust`/`LargeBlockLargeContainer`) + ServerDoWeld (3 ms) account for only 12 ms. ~15 ms inside the loop body is unprofiled — candidates: `Weldable` engine call cost, `BlockSystemAssigningHandler` swap, lock contention with background scan publish, lock-on resolution, or GC pause. Same diagnostic pattern as BUG-105/121: instrumentation only, no behavioral change. Sub-timers planned: `tsCompList` / `tsProjQuery` / `tsInvLookup` (FindMissing internals) and `tsLoopFilter` / `tsLoopAssign` / `tsLoopSelect` (TryWelding loop phases). Follow-up fix ticket per data — most likely projector-buildable cache (similar to BUG-115 broken-block cache) if `tsProjQuery` dominates |
| BUG-129 | High | v2.5.5 | `Helpers/InventoryHelper.cs`, `NanobotSystem.Inventory.cs` | TODO | `PullComponents` source-walk: per missing component, walked all 94 sources via `srcInventory.FindItem(componentId)` engine calls. Profile `20260429214111` showed `pullPickMs=6.509 ms` on `LargeRefinery projected` (out of 6.535 ms total `ServerFindMissingComponents`) — dominated by the source-walk loop. With many missing components on a complex projected block (refinery, assembler), each weld tick fired `N components × 94 source FindItems` = the dominant per-tick spike on welding. **Fix**: source-has-component cache (`SourceHasComponentCache` `TtlCache<MyTuple<long, string>, bool>`, 30 s TTL) added to `Helpers/InventoryHelper.cs`, mirroring the BUG-119 `ConnectionCache` pattern. `PullComponents` checks the cache before `FindItem`; sources cached as "no" are skipped (no engine call). Sources cached as "yes" still call `FindItem` to get the item index for transfer — the saving is on the dominant "no" path. Stale `false` entries → BaR may miss a freshly-stocked source for up to 30 s, benign. Cleanup hooked into existing 2-min `InventoryHelper.Cleanup` sweep. Independent of BUG-126 (welder-full path) and unrelated to user-tunable `WeldingMultiplier` setting (which adjusts per-call pull volume) |
| BUG-127 | High | v2.5.5 | `Mod.cs`, `NanobotSystem.Grinding.cs` | TODO | Player observation: `target.CubeGrid.RemoveBlock(...)` causes spikes when it actually removes the block (5-8 ms on conveyor-connected blocks like batteries/containers — SE-engine cleanup: integrity recalc, conveyor topology refresh, mass / event fanout). BUG-106 caps full-dismounts to 3/tick but **the raze still happened on the same tick as the grind** = 39 ms peak when 3 BaRs simultaneously dismount complex blocks (15 ms decrease + 24 ms raze), well over the 16.7 ms 60-Hz frame budget. **Fix**: deferred raze queue (`Mod._razeQueue`) — grind site enqueues `(SetToConstructionSite + RemoveBlock(target, true))` instead of calling them inline; `Mod.UpdateBeforeSimulation` drains at most `MaxRazesPerTickDefault=1` per tick on the main thread. Decouples engine cleanup work from the grind tick. Blocks sit at `IsDestroyed=true` until drained (1-N ticks lag, ~16-50 ms); existing `ServerTryGrinding` `IsDestroyed`/`FatBlock.Closed` checks already skip them from being re-targeted. Throughput: 60 razes/sec (down from 180), still ~50 minutes per 11k-block grid — raise `MaxRazesPerTickDefault` to 2 if needed. Future enhancement: `IMyCubeGrid.RazeBlocks(List<Vector3I>)` plural-batch coalescing, separate ticket if measured win |
| BUG-126 | High | v2.5.5 | `NanobotSystem.Inventory.cs` | TODO | Player-reported: with 58 BaRs grinding 3 large ships, welder inventories filling up triggered "constant" CPU spikes; freeing cargo + adding push containers restored "regular interval spikes every few seconds." Two hot loops fire even when welder is at capacity: (1) `PullComponents` walks all 94 sources via `FindItem` + `CanTransferItemTo` engine calls before the inevitable `MaxItemsAddable=0` failure bails the function — `remainingVolume` parameter checks transport volume, not welder volume, so the welder-full case wasn't gated; (2) `ServerEmptyTransportInventory` iterates every transport item and calls `welderInventory.MaxItemsAddable` per item even when every result will be 0. Both fixes are pure performance early-outs (no behaviour change — transfers always failed in the welder-full case anyway): `PullComponents` returns false when welder ≥99% full before the source walk; `ServerEmptyTransportInventory` skips the transport-drain loop when `welderFreeVol <= 0`. Independent of BUG-125 (this fires regardless of profiling state) |
| BUG-125 | Critical | v2.5.5 | `Profiling/MethodProfiler.cs`, `Mod.cs` | TODO | Profiler self-overhead is dominating measured spikes. Profile `20260429185841-profiling` (3 large ships, 58 BaRs, sim 0.38 min): top `ServerDoGrind` 10.172 ms with `transportEmptyMs=8.418` (BUG-124 outer measurement), but `ServerEmptyTransportInventory`'s own profiler shows `ms=0.474` for same entity / same timestamp. The 8 ms gap lives inside `MethodProfiler.StopAndLog → Write` (`MethodProfiler.cs:682`): per-line `writer.Flush()` under a global `_syncRoot` lock serializes all 58 BaRs' profile writes through synchronous file I/O. Inner profiler's `elapsedTicks` is captured BEFORE that lock, so the function reports its own work as fast — outer measurements (BUG-124 sub-timers) reveal the true wall cost. Real `ServerDoGrind` work in this scenario is ~1.7 ms; the other ~8 ms is profiler exit overhead. **Fix**: removed per-line `Flush()` in `MethodProfiler.Write`; added `MethodProfiler.FlushAll()` called every 5 s from `Mod.UpdateBeforeSimulation` so a non-`StopSession` close still has most data on disk. Lock-contention angle (single `_syncRoot`) deferred to follow-up if removing Flush isn't sufficient. **User impact**: visible "lag during grinding" should largely disappear with profiling on; without profiling, this overhead never existed |
| BUG-124 | High | v2.5.5 | `NanobotSystem.Grinding.cs` | TODO | `ServerDoGrind` `transportMs` 19 ms spike is NOT in `ServerEmptyTransportInventory` (own profiler shows max 0.189 ms across 2 489 calls). The spike lives elsewhere in the `tsTransport` block at `Grinding.cs:325-338` — most likely `ComputePosition(target) → target.ComputeWorldCenter` called 3 lines after `target.CubeGrid.RemoveBlock(target, true)` removes the block from its grid. Profile session `20260429184659-profiling` (3 large ships, ~11 700 blocks each, 58 BaRs, sim dipped to 0.57): top `ServerDoGrind` 19.7 ms, transportMs=19.134, dismounted=True. Same diagnostic-first pattern as BUG-105/121/122: instrumentation only this ticket — split `tsTransport` into `tsTransportGate` / `tsTransportPos` / `tsTransportSet` / `tsTransportEmpty` sub-timers. Fix follows once dominant segment identified. Likely fix: cache the world center *before* the raze line (block reference still has pre-removal coordinates) instead of recomputing post-removal |
| BUG-123 | Medium | v2.5.5 | `Mod.cs`, `NanobotSystem.Grinding.cs` | TODO | Friendly-damage relation cache. `ServerDoGrind` iterates `Mod.NanobotSystems` (58 entries) per grind, calling `GetUserRelationToOwner` per entry — result is purely a function of (otherBaR.OwnerId, _Welder.OwnerId), independent of the block being ground. Profile `20260429181044` shows max `friendlyMs` 4.813 ms (LargeHalfSlopeArmorBlock dismount) and 3.897 ms (LargeBlockArmorBlock) outliers — substantial chunks of the per-grind cost on spike ticks. BUG-105 measured ~70 µs total in a typical session and dismissed; current data shows under different SE engine state the per-call cost spikes to 50-100 µs each. Cache survives both regimes. Fix: `Mod._FriendlyBaRsByOwner` `ConcurrentDictionary<long, List<NanobotSystem>>` rebuilt every 5 s in `Mod.UpdateBeforeSimulation`; grind path = single dict lookup + iterate. 5 s staleness inside the 30 s `FriendlyDamageTimeout` envelope. Realizes recommendation #1 from `iterative-questing-oasis.md` |
| BUG-115 | High | v2.5.4 | `NanobotSystem.Welding.cs`, `NanobotSystem.cs` | Done | "BaRs in [large/offline] world try to weld but never actually weld anything." Profile session `notworking` (60 s, 3 BaRs) showed 363 `ServerTryWelding` calls / 88 `ServerFindMissingComponents` calls / **0 `ServerDoWeld` calls in the summary** — the log file did not even exist. Root cause **confirmed in-field**: SE in **offline mode** does not populate `MySessionComponentGameInventory`'s DLC entitlement state (no Steam connection to query), so `MyProjectorBase.BuildInternal` → `ValidateArmor` → `HasArmor(armorId, steamId)` dereferences null internal-state and throws NRE on every `proj.Build()`. Exception bypassed `MethodProfiler.StopAndLog` (no try/finally in `ServerDoWeld`), bubbled through `ServerTryWelding` and `ServerTryWeldingGrindingCollecting`, and was silently swallowed by `UpdateBeforeSimulation10_100`'s top-level `catch (Exception)` at `Update.cs:198`. Lock-on block stayed locked across ticks → BaR re-locked the same broken block forever, never welded anything. **Player verification**: switching the same world to online mode made the NRE stop without any code change. Earlier hypotheses (BuiltBy resolution, modded armor variants, big-world player-table state) all ruled out by the offline/online toggle. **Fix layer 1**: wrapped `proj.Build()` in `try/catch (NullReferenceException)`, set `targetData.Ignore = true` on catch, returned `false`, emitted a `ServerDoWeld` profiler line with `earlyExit=projBuildNRE`. **Fix layer 2**: per-`TargetBlockData` `Ignore` is wiped each background scan rebuild (~2 s), so warnings spammed on every cycle. Added per-BaR persistent `HashSet<string> _BrokenProjBuildKeys` (key `gridEntityId:position`, mirroring `BlockSystemAssigningHandler`); `Weldable()` short-circuits to `Ignore=true` for keys in the set; the catch uses `_BrokenProjBuildKeys.Add(key)` return value to gate `Logging.Instance.Write` so warnings appear **once per broken block per BaR session**. **Diagnostic logging**: NRE warning now captures `block.subtype/typeId/pairName/PCU/fromMod/modName`, `passed.{owner,builder,builtBy}`, `passed.builtBy.online` (via `MyAPIGateway.Players.GetPlayers` predicate filter), `barSlim.builtBy{,.online}`, projector `entityId`/`grid` so any future occurrence is fully self-describing. Defense-in-depth — not required when online, but keeps the BaR functional offline and survives any future SE state where the entitlement table happens to be empty |
| BUG-260501.1 | High | v2.5.4 | `Cluster/ScanClusterCoordinator.cs`, `NanobotSystem.Scanning.cs` | Done | Toggling Nearest↔Farthest (or any `ClusterRelevantFlags` bit) takes ~10–60 s+ to switch the cluster's sorted target list instead of the FEAT-080 promised ~1–2 s. Profile `20260501191840-profiling` (3 large ships, 58 BaRs, 30 s): user toggled at T+17–19 s; `ImmediateRescanTrigger` fired 58× (trigger works) but `AsyncClusterScan` ran only 3× total (all warmup) with **zero scans for the 13 s after the toggle**, `SortAndCapGridCandidates` ran 2× total (no re-sort post-toggle). Root cause: `GrindNearFirst` is in `ClusterRelevantFlags` (`ScanClusterCoordinator.cs:28`), so toggling it changes the cluster-key hash and the next ~2 s `RebuildClusters` reshuffles the toggling BaR into a different cluster with a different elected coordinator. `TriggerImmediateRescan(reason, bypassDebounce: true)` set `_rescanForced=true` on `this` and on the *current* coordinator, but the *new* coordinator never received the trigger — its saturated-skip gate (`forceRescan = _rescanForced && timeSinceFullScan >= 5 s` → false) suppresses every subsequent tick until the target list drains below saturation or `MaxScanSkipDuration = 60 s` elapses. The BUG-139 self-flag-on-`this` defence only works when `this` becomes the new coordinator; it does nothing when reshuffled into a cluster where someone else coordinates. **Fix**: `ScanClusterCoordinator.RebuildClusters` adds a propagation pass after `ElectCoordinator()` — for each cluster, if any non-coordinator member carries `_rescanForced=true`, call new `coord.InheritForcedRescan()` (sets `_rescanForced=true`, zeroes `_lastFullScanTime` and `_LastTargetsUpdate`) and clear the member's flag (consumed). Cost: zero on the fast-path skip; full rebuild path adds one bool read per member per cluster, no allocation. Existing BUG-139 self-flag in `TriggerImmediateRescan` kept as the immediate-trigger path when no reshuffle happens |
| BUG-260502.1 | High | v2.5.4 | `Handlers/SafeZoneHandler.cs` | Done | REVIEW-260502.1 #1, re-flagged by REVIEW-260511.1 #1. `GetActionsAllowedForSystem` initialised `ActionsState` with all flags `true` and a broad catch returned those defaults on any exception → safe-zone restrictions silently bypassed when the lookup threw. Fix: success path unchanged (no-zone → permissive is correct), catch path now returns a fresh `ActionsState` with all three flags `false` and logs the exception at Error level. Behaviour change: on a transient engine exception the BaR pauses weld/grind/build until next tick instead of briefly running unrestricted |
| BUG-260502.2 | High | v2.5.4 | `Handlers/SafeZoneHandler.cs` | Done | REVIEW-260502.1 #2, re-flagged by REVIEW-260511.1 #2. `IsProtectedFromGrinding` catch path wrote `false` (not protected) to `ProtectedFromGrindingCache` (15 s TTL) and returned `false` — a transient exception allowed grinding on a protected block for up to 15 s. Fix: catch now returns `true` (treat as protected), skips the cache write so the next call retries clean against live engine state, and logs at Error level (the original catch was silent) |
| BUG-260502.3 | Low | v2.5.4 | `Chat/ChatHandler.cs`, `Handlers/HudHandler.cs`, `Handlers/NetworkMessagingHandler.cs` | TODO | REVIEW-260502.1 #3. Three sites (`ChatHandler.cs:286`, `HudHandler.cs:667`, `NetworkMessagingHandler.cs:364`) compare `PromoteLevel.ToString() == "Admin" \|\| ...`; `NetworkMessagingHandler.cs:158` already uses direct enum compare with `// REF-4:` comment. Replace ToString sites with enum compare; optionally extract a single helper |
| BUG-260502.4 | Low | v2.5.4 | `Handlers/FriendlyRelationsHandler.cs`, `Managers/PeriodicMaintenanceScheduler.cs` | TODO | REVIEW-260502.1 #4. `FriendlyRelationsHandler.Rebuild` is `O(distinct_owners × N)` (not `O(N²)` as the bot suggested — `seenOwners.Add` dedups) and runs every 5 s. On 60 BaRs / 60 owners worst case = 3,600 GetUserRelationToOwner engine calls per cycle. Fix options: extend interval to 15 s, hook `Factions.FactionStateChanged` for dirty-flag rebuild, or incremental rebuild |
| BUG-260502.5 | Low | v2.5.4 | `Managers/BackgroundTaskQueue.cs`, `Managers/PeriodicMaintenanceScheduler.cs`, `Handlers/NetworkMessagingHandler.cs` | TODO | REVIEW-260502.1 #5. Empty `catch { }` blocks in runtime paths (`BackgroundTaskQueue.cs:84`, `PeriodicMaintenanceScheduler.cs:49-54,65-66,89`, `NetworkMessagingHandler.cs:381,392,404,415`) silently drop bugs. `Mod.UnloadData` blanket catches stay (intentional). Fix: replace runtime catches with `Logging.Instance.Write` at Error level |
| BUG-260502.6 | Low | v2.5.4 | `NanobotSystem.cs:341`, `Cluster/ScanClusterCoordinator.cs:82,88` | TODO | REVIEW-260502.1 #7. `_lastClusterKey` string field is vestigial after FEAT-072 switched to `_lastClusterKeyHash` for the fast-path. Written at two sites, never read. Delete the declaration + writes |
| BUG-260502.7 | Low | v2.5.4 | `Handlers/HudHandler.cs:583` | TODO | REVIEW-260502.1 #8. `BuildStats` allocates a fresh `List<IMyPlayer>` per call just to read `Count`. Only runs when DebugMode/profiling active so impact is bounded. Fix: pool a static scratch list (mirrors `_adminCheckPlayers` pattern in `NetworkMessagingHandler.cs:156`) |
| BUG-260511.1 | Medium | v2.5.4 | `NanobotSystem.Grinding.cs` | Done | Cosmetic grind transport timer kept restarting. With the multi-action wrapper (FEAT-260511.6) firing several `ServerDoGrind` calls per cycle, the transport-gate block unconditionally overwrote `State.CurrentTransportStartTime = playTime` on every iteration → cosmetic timer pinned to "now", particles streamed out forever without "arriving" at the BaR. Fix: only seed the cosmetic transport when none is already in flight; the inventory transfer (`ServerEmptyTransportInventory`) still runs every gate-open. Welding-side equivalent (`ServerFindMissingComponents`) already had this gate so welding was unaffected. Fix is kept even though FEAT-260511.6 is now `MaxActionsPerCycle = 1` — same code path is exercised by full-grid-grind across successive ticks |
| BUG-260511.2 | Medium | v2.5.4 | `NanobotSystem.Grinding.cs`, `NanobotSystem.Update.cs` | Done | Grind picker skipped blocks at integrity 0 with components remaining. Player report: wheels and other attachable blocks ended up at 0 % integrity with components still in the construction stockpile, never fully ground or razed. Root cause: picker (`ServerTryGrinding:93`) filtered with `!IsDestroyed`, but `IsDestroyed` is true as soon as `Integrity <= 0` regardless of stockpile state. Same wrong filter in the BUG-165 drop-from-target-list optimization (`Grinding.cs:142`) and in the FEAT-260511.3 visualization filter (`IsLiveGrindTarget`). Fix: replaced all three with `IsFullyDismounted` (true only when integrity is 0 AND stockpile is empty) so the BaR keeps stripping components after integrity hits 0; once truly dismounted the block flows through the existing RazeQueueHandler path |
| BUG-260511.3 | Medium | v2.5.4 | `NanobotSystem.Grinding.cs`, `Handlers/RazeQueueHandler.cs` | Done | Follow-up to BUG-260511.2. After the picker correctly skipped already-`IsFullyDismounted` blocks, those blocks never reached `RazeQueueHandler.Enqueue` (only called from inside `ServerDoGrind`) — skeleton orphans (e.g. wheels on a detached subgrid after the suspension was destroyed) stayed on the grid forever. Player report: session restart let the BaR clean up *some* blocks (those still with components) but not the truly-empty ones. Fix: picker's else branch now calls `RazeQueueHandler.Enqueue(block)` so the cleanup path sees the orphan. Added `HashSet<BlockKey>` dedup to `RazeQueueHandler.Enqueue` so 58 BaRs all spotting the same orphan don't pile up thousands of redundant queue entries; key freed when `Process` dequeues so a re-engineered block (re-welded, then re-dismounted) can be re-enqueued |
| BUG-260511.4 | High | v2.5.4 | `NanobotSystem.Grinding.cs` | Done | Grind picker leaked assignments onto every iterated block — `AssignToSystem` is a **claim**, not a check, so the iteration's `!AssignToSystem` skip line was claiming every block walked past on the way to the chosen one. Other BaRs then saw those blocks as "claimed by someone" for `AssignmentTtlSeconds` (2-8 s) and skipped them. Result: on a 58-BaR fleet, one BaR's single picker cycle could hold tens of phantom claims, forcing other BaRs to skip otherwise-free work. Welding (`ServerTryWelding:131-141`) already had the correct two-phase pattern; grinding was inconsistent. Fix: use `IsAssignedToOtherSystem` (pure read) during the iteration, call `AssignToSystem` only on the chosen target after the loop breaks, with race-handling (drop the target if another BaR grabbed it between read and write). Each BaR now holds exactly one assignment per cycle, the TTL-refresh path is preserved as long as the picker keeps picking the same block on successive cycles |
| BUG-260511.12 | Medium | v2.5.4 | `Models/SyncBlockSettings.cs`, `Models/SyncModSettings.cs` | Done | Codex review: deprecated `WorkModes.GrindIfWeldGetStuck` bit was never migrated out of `Mod.Settings.Welder.AllowedWorkModes`. Per-block `WorkMode` setter migrates the field but the clamp's fallback chain (WeldBeforeGrind / GrindBeforeWeld only) couldn't reselect a valid mode when the allow-mask contained only the deprecated bit — terminal combobox went empty, per-block `WorkMode` stayed outside the allow-mask. Same shape for masks containing only `WeldOnly` or `GrindOnly`. Two-layer fix: (1) `ValidateAndClamp` folds `GrindIfWeldGetStuck` into `WeldBeforeGrind` on every load + receive, migrates `WorkModeDefault` similarly, restores full default mask if empty; (2) per-block clamp extends fallback chain to also cover `WeldOnly` / `GrindOnly` and locally tolerates the deprecated bit |
| BUG-260511.11 | Medium | v2.5.4 | `NanobotSystem.Scanning.Block.cs` | Done | Codex review: `ComputeScanParamsHash` covered the safe-zone allow-flags but not `State.IsShielded`. Since `AsyncAddBlockIfGrindTarget` rejects off-grid autogrind when shielded, a shield toggle within the 3 s `GridScanCache` TTL would replay cached candidates computed under the old shield state — briefly keeping forbidden targets (shield just up) or excluding now-permitted ones (shield just down). Fix: hash `State.IsShielded` into the params hash using the same FNV pattern as the safe-zone bools. Ticket also documents the forward-compat link to BUG-260511.9 — both the state fingerprint and the scan-cache hash must keep up with any new state-dependent filter |
| BUG-260511.10 | Medium | v2.5.4 | `Handlers/RazeQueueHandler.cs` | Done | Codex review: `RazeQueueHandler.Process` removed each block's dedup key only after the null-grid `continue` path, AND derived the key from `target.CubeGrid.EntityId` — so when a block lost its grid between enqueue and dequeue (detached subgrid, world cleanup), the key got stranded permanently in `_pendingKeys`. Slow leak that eventually blocks future enqueues for the same grid+position. Fix: introduced a tiny `QueueEntry` struct that pairs the block with its precomputed key at enqueue time; `Process` removes `entry.Key` unconditionally before any skip path |
| BUG-260511.9 | High | v2.5.4 | `NanobotSystem.Operations.cs` | Done | Codex review (extended audit). Same shape as BUG-260511.8 for **eight** more fields: `SafeZoneAllowsWelding` / `SafeZoneAllowsGrinding` / `SafeZoneAllowsBuildingProjections` / `IsShielded` (the four Codex flagged — HUD gating sticks when entering/leaving a zone-bubble while idle), plus `LastTransportTarget`, `CurrentTransportIsPick`, `CurrentTransportTime`, `CurrentTransportStartTime`. Audited every `Changed = true` setter in `SyncBlockState` against `ComputeStateFingerprint` to confirm parity. Transport timing fields seeded once per transport (BUG-260511.1 gate) so hashing them does not churn the fingerprint. Going forward, the fingerprint and the `Changed` surface must stay in sync — documented as a forward-compatibility rule in the ticket |
| BUG-260511.8 | High | v2.5.4 | `NanobotSystem.Operations.cs` | Done | Codex review: `ComputeStateFingerprint` did not include `State.CurrentWeldingBlock` / `CurrentGrindingBlock`. Their setters flip `State.Changed = true` on a real lock-on target change, but the BUG-150 fpUnchanged early-return path immediately suppressed the send because the fingerprint hadn't moved. Clients kept stale beam/effect targets until some other hashed field shifted. Fix: hash both blocks via `gridId + Position` (matches the same-physical-block identity tuple the setters use, so a reference refresh on the same block still suppresses correctly). No allocation, no engine call |
| BUG-260511.7 | High | v2.5.4 | `Managers/PeriodicMaintenanceScheduler.cs` | Done | Codex review: three periodic maintenance tasks were dispatched onto `MyAPIGateway.Parallel.StartBackground` but touched engine APIs that aren't thread-safe — `FriendlyRelationsHandler.Rebuild` (`GetUserRelationToOwner`), `GridOwnershipCacheHandler.Update` (`MyAPIGateway.Entities.TryGetEntityById`, `IMyCubeGrid.Closed`), and `SafeZoneHandler.CleanupSafeZones` (reads `MySafeZone.MarkedForClose`/`Closed` plus shares a main-thread-only scratch buffer). Latent stability risk under load. Fix: added `MainThread` flag to `PeriodicTask`; `TryFire` now runs flagged tasks inline. The bundled `_friendlyAndProfiler` task split into `_friendlyRebuild` (main-thread, 5 s) and `_profilerFlush` (background, 5 s — plain .NET file I/O is fine off-thread). `_ttlCleanup` unchanged (TTL caches are thread-safe) |
| BUG-260511.6 | Low | v2.5.4 | `Mod.cs`, `NanobotSystem.cs` | Done | Auto `StaggerGroupCount` counted every placed BaR (including disabled ones). Player report: 2 enabled + 58 disabled BaRs reported `Stagger=3`. Fix: count only entries with `_Welder.Enabled == true` via new public `NanobotSystem.IsEnabled` accessor. Added a ~1 s frame-counter cache around the dictionary enumeration so the per-BaR Update10 call doesn't re-enumerate every fire (60 BaRs × 6 fires/s = 360 calls/s before caching). Explicit `StaggerGroupCount` override still bypasses both count and cache |
| BUG-260511.5 | Low | v2.5.4 | `NanobotSystem.Update.cs` | Done | Solo-cluster (1 BaR) stagger inherited the mod-wide value directly, so on a world with 11+ placed BaRs the lone active BaR fired only every 3rd cycle — 2 fires/sec at WorkSpeed=10 instead of 6. Felt sluggish to the player. Fix: cap solo-cluster `effectiveGroups` at 2 (`Math.Min(modWideStagger, 2)`). Small clusters (2-5) already collapse to 1 and large clusters (≥6) still scale up to mod-wide max; only the solo path changes. Admin override via `/nanobars config set StaggerGroupCount 3` still applies if explicitly desired |
